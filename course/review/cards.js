// AUTO-GENERATED by generate_cards.py — do not edit manually.
// Run: python generate_cards.py

window.CARDS = [
  {
    "id": "01_foundations_quiz_q01",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q1.** What is the difference between a compiler error and a linker error? Give one example of each.",
    "back": "**Compiler error** — caught during compilation of a single `.cpp` file. The compiler checks syntax and types but knows nothing about other translation units.\nExample: calling a function with wrong argument types, using an undeclared variable.\n\n**Linker error** — caught when combining `.o` files. The linker cannot find a definition that was declared but never implemented.\nExample: `undefined reference to 'compute_hypotenuse(double, double)'` — the declaration was in a header but no `.o` file contained the definition."
  },
  {
    "id": "01_foundations_quiz_q02",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q2.** You put the definition of a free function in a header and include it in three `.cpp` files. What happens at link time and why?",
    "back": "The linker sees three copies of the same symbol (one from each `.o` file) and raises a **multiple definition** error. Each `.cpp` that includes the header gets its own copy after preprocessing. The linker requires exactly one definition per symbol.\n\nFix: move the definition to a `.cpp` file, or mark the function `inline`."
  },
  {
    "id": "01_foundations_quiz_q03",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q3.** What does `#pragma once` do? Why must it appear at the top of a header?",
    "back": "It tells the compiler to include this file at most once per translation unit, preventing redefinition errors from repeated inclusion (direct or transitive). It must appear before any code so it takes effect before any declarations are processed."
  },
  {
    "id": "01_foundations_quiz_q04",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q4.** Explain the difference between `const int* p` and `int* const p`.",
    "back": "`const int* p` — pointer to const int. Cannot modify `*p`. Can reassign `p` to point elsewhere.\n\n`int* const p` — const pointer to int. Can modify `*p`. Cannot reassign `p`.\n\nRead right-to-left: `const int* p` → \"p is a pointer to const int\"."
  },
  {
    "id": "01_foundations_quiz_q05",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q5.** Why should you never write `using namespace std;` in a header file?",
    "back": "`#include` pastes the header into every file that includes it. The `using namespace` then silently applies to all those translation units, potentially pulling in names that clash with names in those files or other headers. This can cause ambiguous overloads or silent changes in which function gets called."
  },
  {
    "id": "01_foundations_quiz_q06",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q6.** What does this print?\n\n```cpp\n#include <iostream>\n#include <cstdint>\nint main() {\n    uint8_t x = 200;\n    uint8_t y = 100;\n    uint8_t z = x + y;\n    std::cout << static_cast<int>(z) << \"\\n\";\n}\n```",
    "back": "**44** — `200 + 100 = 300`. `uint8_t` holds 0–255. `300 % 256 = 44`. Silent wraparound."
  },
  {
    "id": "01_foundations_quiz_q07",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q7.** What does this print?\n\n```cpp\n#include <iostream>\nvoid increment(int x) { x += 1; }\nint main() {\n    int a = 5;\n    increment(a);\n    std::cout << a << \"\\n\";\n}\n```",
    "back": "**5** — `increment` receives a copy of `a`. The original is not modified. To modify the caller's variable, take `int&` or `int*`."
  },
  {
    "id": "01_foundations_quiz_q08",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q8.** What does this print?\n\n```cpp\n#include <iostream>\nnamespace A { int x = 1; }\nnamespace B { int x = 2; }\nint main() {\n    using namespace A;\n    std::cout << x << \"\\n\";\n    {\n        using namespace B;\n        std::cout << x << \"\\n\";\n    }\n    std::cout << x << \"\\n\";\n}\n```",
    "back": "Prints `1`, then a **compile error** on the second `cout`. Inside the inner block both `A::x` and `B::x` are in scope — the compiler cannot choose between them (ambiguous). The third line would print `1` if the program compiled."
  },
  {
    "id": "01_foundations_quiz_q09",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q9.** Find the bug and explain what happens at runtime.\n\n```cpp\nvoid print_first(const int* arr, size_t size) {\n    std::cout << arr[0] << \"\\n\";\n}\nint main() {\n    int* p = nullptr;\n    print_first(p, 0);\n}\n```",
    "back": "Null pointer dereference. `p` is `nullptr`. `arr[0]` dereferences it unconditionally — undefined behaviour, typically a segfault. Fix: guard with `if (!arr || size == 0) return;`."
  },
  {
    "id": "01_foundations_quiz_q10",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q10.** This CMakeLists.txt fails to build. Find and fix the mistake.\n\n```cmake\nadd_library(sensor src/sensor.cpp)\nadd_executable(demo src/main.cpp)\ntarget_include_directories(sensor PRIVATE include)\ntarget_link_libraries(demo PRIVATE sensor)\n```\n\n`main.cpp` includes `\"sensor.hpp\"` from `include/` but the build fails with \"file not found\".",
    "back": "`PRIVATE` means the include path is only available when compiling `sensor.cpp` — it is not propagated to `demo`. Fix: change to `PUBLIC`.\n\n```cmake\ntarget_include_directories(sensor PUBLIC include)\n```\n\nRule: if your library's headers are part of its public API (included by consumers), use `PUBLIC`."
  },
  {
    "id": "01_foundations_quiz_q11",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q11.** Complete this function without using any library function other than `std::acos` and `std::sqrt`.\n\n```cpp\ndouble angle_between(double ax, double ay, double bx, double by) {\n    // TODO\n}\n```",
    "back": "```cpp\ndouble angle_between(double ax, double ay, double bx, double by) {\n    double dot   = ax*bx + ay*by;\n    double mag_a = std::sqrt(ax*ax + ay*ay);\n    double mag_b = std::sqrt(bx*bx + by*by);\n    double cos_theta = dot / (mag_a * mag_b);\n    // clamp to [-1,1] — floating-point can exceed bounds and cause NaN\n    cos_theta = cos_theta < -1.0 ? -1.0 : (cos_theta > 1.0 ? 1.0 : cos_theta);\n    return std::acos(cos_theta);\n}\n```"
  },
  {
    "id": "01_foundations_quiz_q12",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "quiz",
    "front": "**Q12.** Write the CMakeLists.txt for this layout. C++17, library named `filters_lib`, executable named `robot_filter`. Both library sources and `main.cpp` need `include/`.\n\n```\nproject/\n├── CMakeLists.txt\n├── include/filters.hpp\n└── src/\n    ├── filters.cpp\n    ├── ekf.cpp\n    └── main.cpp\n```",
    "back": "```cmake\ncmake_minimum_required(VERSION 3.15)\nproject(robot_filter)\nset(CMAKE_CXX_STANDARD 17)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\n\nadd_library(filters_lib src/filters.cpp src/ekf.cpp)\ntarget_include_directories(filters_lib PUBLIC include)\n\nadd_executable(robot_filter src/main.cpp)\ntarget_link_libraries(robot_filter PRIVATE filters_lib)\n```"
  },
  {
    "id": "01_foundations_concept_1_1_the_compilation_pipeline",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.1 The Compilation Pipeline",
    "back": "When you run `g++ main.cpp -o program`, four things happen in sequence.\n\n### Step 1 — Preprocessor\n\nThe preprocessor runs before compilation. It handles all lines starting with `#`. It does text substitution — it knows nothing about C++ syntax.\n\n```cpp\n#include <iostream>      // copy-paste the entire iostream header here\n#define PI 3.14159       // replace every occurrence of PI with 3.14159\n\nint main() {\n    double area = PI * 5 * 5;   // becomes: double area = 3.14159 * 5 * 5;\n}\n```\n\nSee the preprocessed output:\n```bash\ng++ -E main.cpp -o main.i\n```\n\n### Step 2 — Compiler\n\nTranslates preprocessed C++ into object code (`.o` files). Each `.cpp` compiles independently. The compiler checks syntax and types but does **not** resolve references between files.\n\n```bash\ng++ -c main.cpp -o main.o    # compile only, do not link\n```\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "01_foundations_concept_1_2_headers_vs_source_files",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.2 Headers vs Source Files",
    "back": "| File | Contains | Why |\n|------|----------|-----|\n| `.hpp` header | **Declarations** | Tells the compiler what exists |\n| `.cpp` source | **Definitions** | The actual code, compiled once |\n\nA **declaration** says \"this exists, here is its type\":\n```cpp\ndouble dot_product(double ax, double ay, double bx, double by);\n```\n\nA **definition** provides the body:\n```cpp\ndouble dot_product(double ax, double ay, double bx, double by) {\n    return ax * bx + ay * by;\n}\n```\n\nIf you put a definition in a header and include it from two `.cpp` files, both `.o` files contain a copy — the linker sees two definitions of the same symbol and errors: `multiple definition of 'dot_product'`.\n\n### What belongs in headers\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "01_foundations_concept_1_3_include_guards_and_pragma_once",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.3 Include Guards and `#pragma once`",
    "back": "Without a guard, including `b.hpp` through two paths causes its contents to appear twice — redefinition error.\n\n### Include guards (portable)\n```cpp\n#ifndef MYPROJECT_B_HPP\n#define MYPROJECT_B_HPP\n// ... contents ...\n#endif\n```\n\n### `#pragma once` (simpler, universally supported)\n```cpp\n#pragma once\n// ... contents ...\n```\n\nUse `#pragma once` for new code. All major robotics codebases use it.\n\n---"
  },
  {
    "id": "01_foundations_concept_1_4_basic_types",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.4 Basic Types",
    "back": "| Type | Size | Use |\n|------|------|-----|\n| `bool` | 1 byte | true / false |\n| `int` | 4 bytes | general integer |\n| `double` | 8 bytes | double-precision float — use for physics |\n| `float` | 4 bytes | single-precision — use for sensor data |\n| `size_t` | 8 bytes | sizes and indices |\n\n### Fixed-width types (`<cstdint>`)\n```cpp\n#include <cstdint>\nuint8_t   byte_val = 255;     // 0 to 255\nint16_t   short_val = -1000;\nuint32_t  count = 0;\nint64_t   timestamp_ns = 0;   // nanoseconds since epoch\n```\n\n### Implicit conversion and narrowing\n```cpp\ndouble d = 3.99;\nint    i = d;           // truncates to 3 — silent data loss\nuint8_t b = 300;        // wraps to 44 — silent data loss\n\n// Use explicit casts when you intend conversion:\nint i2 = static_cast<int>(d);\n```\n\n---"
  },
  {
    "id": "01_foundations_concept_1_5_const",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.5 `const`",
    "back": "### `const` variables\n```cpp\nconst double GRAVITY = 9.81;\nGRAVITY = 10.0;   // compile error\n```\n\n### `const` with pointers — two distinct meanings\n```cpp\nint x = 5, y = 10;\n\nconst int* p1 = &x;   // pointer to const int\n*p1 = 20;             // error: cannot modify value\np1  = &y;             // OK: can point elsewhere\n\nint* const p2 = &x;   // const pointer to int\n*p2 = 20;             // OK: can modify value\np2  = &y;             // error: cannot repoint\n```\n\nRead right-to-left: `const int* p` = \"p is a pointer to const int\".\n\n### `const` references for function parameters\n```cpp\nvoid process(const std::vector<double>& data) { ... }  // no copy\n```\n\nRule: pass cheap types (int, double) by value. Pass everything else by `const&`.\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "01_foundations_concept_1_6_references_vs_pointers",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.6 References vs Pointers",
    "back": "```cpp\nint x = 42;\nint* ptr = &x;    // pointer: stores the address\nint& ref = x;     // reference: another name for x\n\n*ptr = 10;        // modify through pointer\nref  = 10;        // modify through reference — no * needed\n```\n\n| Property | Reference | Pointer |\n|----------|-----------|---------|\n| Can be null | No | Yes (`nullptr`) |\n| Can be reseated | No | Yes |\n| Must be initialised | Yes | No |\n| Arithmetic | No | Yes |\n\n**Use a reference when:** the thing is guaranteed to exist (function parameters, range-for).\n**Use a pointer when:** null is a valid state, or you need to swap what you're pointing to.\n\nNever dereference a pointer without checking it:\n```cpp\nvoid process(const Scan* scan) {\n    if (!scan) return;\n    // safe to use scan->...\n}\n```\n\n---"
  },
  {
    "id": "01_foundations_concept_1_7_function_overloading",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.7 Function Overloading",
    "back": "Multiple functions with the same name, different parameter types. The compiler picks at compile time — no runtime cost.\n\n```cpp\ndouble magnitude(double x, double y)           { return std::sqrt(x*x + y*y); }\ndouble magnitude(double x, double y, double z) { return std::sqrt(x*x + y*y + z*z); }\n\nmagnitude(3.0, 4.0);          // calls 2D version\nmagnitude(1.0, 2.0, 2.0);     // calls 3D version\n```\n\nCompiler prefers exact match over implicit conversion. If two conversions are equally good: ambiguous → compile error.\n\n---"
  },
  {
    "id": "01_foundations_concept_1_8_namespaces",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.8 Namespaces",
    "back": "```cpp\nnamespace math {\n    double dot(double ax, double ay, double bx, double by);\n}\n\n// Usage options:\ndouble d = math::dot(1.0, 0.0, 0.0, 1.0);   // fully qualified — always safe\n\nusing math::dot;      // imports one name — OK inside a function\nusing namespace math; // imports everything — NEVER in a header\n```\n\n**Never `using namespace X;` in a header.** It pollutes every file that includes it.\n\n### Anonymous namespaces — file-local linkage\n```cpp\nnamespace {\n    double clamp01(double x) { ... }  // invisible outside this .cpp\n}\n```\n\n---"
  },
  {
    "id": "01_foundations_concept_1_9_cmake_basics",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: 1.9 CMake Basics",
    "back": "```cmake\ncmake_minimum_required(VERSION 3.15)\nproject(robot_math)\nset(CMAKE_CXX_STANDARD 17)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\n\nadd_library(mathutils src/mathutils.cpp)\ntarget_include_directories(mathutils PUBLIC include)\n\nadd_executable(main_program src/main.cpp)\ntarget_link_libraries(main_program PRIVATE mathutils)\n```\n\nBuild:\n```bash\ncmake -B build\ncmake --build build\n./build/main_program\n```\n\n### `PUBLIC` vs `PRIVATE` vs `INTERFACE`\n\n| Keyword | Meaning |\n|---------|---------|\n| `PRIVATE` | Only this target |\n| `PUBLIC` | This target AND anything linking it |\n| `INTERFACE` | Only things linking this target (not the target itself) |\n\nIf your library's headers are included by consumers, use `PUBLIC` on `target_include_directories`.\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "01_foundations_concept_key_takeaways",
    "module": "01_foundations",
    "moduleName": "Foundations",
    "type": "concept",
    "front": "Explain: Key Takeaways",
    "back": "1. **Pipeline**: preprocessor → compiler → linker. \"Undefined reference\" = linker error.\n2. **Headers = declarations, source = definitions.** Definitions in headers cause multiple-definition errors.\n3. **`#pragma once`** in every header.\n4. **Use `double`** for physics. Use fixed-width types (`int64_t`, `uint8_t`) when size matters.\n5. **`const` is correctness**, not style. Mark parameters, member functions, and variables `const` wherever possible.\n6. **`const&` for function parameters** — no copy.\n7. **References can't be null.** Use pointers when null is valid.\n8. **Never `using namespace X;` in a header.**\n9. **CMake `PUBLIC`/`PRIVATE`** — get this right or consumers won't find your headers.\n\n---\n\n→ [Exercises](exercises/) → [Quiz](quiz.md)"
  },
  {
    "id": "02_classes_oop_raii_quiz_q01",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q1 — Rule of Three / Rule of Five\n\nA class `SensorBuffer` owns a raw `float*` array allocated on the heap. You write a custom destructor to `delete[]` it.\n\n(a) Why does the compiler-generated copy constructor cause undefined behaviour for this class?\n(b) List the five special member functions that form the Rule of Five.\n(c) If you don't need copying at all, what is the cleanest way to express that?",
    "back": "**(a)** The compiler-generated copy constructor performs a **shallow copy**: it copies the pointer value, not the data it points to. After `SensorBuffer b = a;`, both `a.data_` and `b.data_` point to the same heap allocation. When `b` is destroyed it calls `delete[] b.data_`. When `a` is then destroyed it calls `delete[] a.data_` on the same memory — this is a **double-free**, which is undefined behaviour and typically a crash.\n\n**(b)** The Rule of Five:\n1. Destructor\n2. Copy constructor\n3. Copy assignment operator\n4. Move constructor\n5. Move assignment operator\n\n**(c)** Delete the copy constructor and copy assignment operator explicitly:\n\n```cpp\nSensorBuffer(const SensorBuffer&)            = delete;\nSensorBuffer& operator=(const SensorBuffer&) = delete;\n```\n\nThis makes any accidental copy a **compile error** instead of a runtime crash. You would also typically `= default` or implement the move operations so the object is still transferable."
  },
  {
    "id": "02_classes_oop_raii_quiz_q02",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q2 — RAII and Exception Safety\n\nExplain why the following function is not exception-safe, then rewrite it using a RAII wrapper. Your rewrite must guarantee the file is closed even if `process(f)` throws.\n\n```cpp\nvoid read_sensor_log(const char* path) {\n    FILE* f = std::fopen(path, \"r\");\n    if (!f) throw std::runtime_error(\"open failed\");\n\n    process(f);    // may throw\n\n    std::fclose(f);\n}\n```",
    "back": "**Why it's unsafe:** If `process(f)` throws an exception, the call stack unwinds and `std::fclose(f)` is never reached. The file handle leaks for the lifetime of the process. A `return` statement added before `fclose` for any early-exit path causes the same problem.\n\n**RAII rewrite:**\n\n```cpp\nclass FileGuard {\npublic:\n    explicit FileGuard(const char* path, const char* mode)\n        : file_{std::fopen(path, mode)}\n    {\n        if (!file_) throw std::runtime_error(\"open failed\");\n    }\n\n    ~FileGuard() {\n        if (file_) std::fclose(file_);\n    }\n\n    FileGuard(const FileGuard&)            = delete;\n    FileGuard& operator=(const FileGuard&) = delete;\n\n    FILE* get() { return file_; }\n\nprivate:\n    FILE* file_;\n};\n\nvoid read_sensor_log(const char* path) {\n    FileGuard f{path, \"r\"};   // acquired here\n    process(f.get());          // may throw — doesn't matter\n}   // FileGuard destructor runs here whether we exit normally or via exception\n```\n\nThe destructor is guaranteed to run on any scope exit — normal return, exception, or early return. The file cannot leak."
  },
  {
    "id": "02_classes_oop_raii_quiz_q03",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q3 — Virtual Destructor\n\nConsider this code:\n\n```cpp\nclass Sensor {\npublic:\n    ~Sensor() { std::cout << \"Sensor dtor\\n\"; }\n    virtual void read() = 0;\n};\n\nclass Lidar : public Sensor {\npublic:\n    ~Lidar() { std::cout << \"Lidar dtor\\n\"; }\n    void read() override { std::cout << \"Lidar reading\\n\"; }\nprivate:\n    float* scan_buffer_ = new float[1000];\n};\n\nint main() {\n    Sensor* s = new Lidar{};\n    delete s;\n}\n```\n\n(a) What output does this program produce, and what resource problem occurs?\n(b) What single change fixes it?",
    "back": "**(a)** Output:\n\n```\nSensor dtor\n```\n\nOnly `~Sensor()` runs. Because `Sensor::~Sensor` is not `virtual`, the compiler resolves the destructor call statically through the `Sensor*` pointer and calls `~Sensor()` directly — `~Lidar()` is **never called**. The `scan_buffer_` allocated by `Lidar` is never `delete[]`'d, causing a **memory leak**. This is undefined behaviour.\n\n**(b)** Declare the destructor `virtual` in the base class:\n\n```cpp\nvirtual ~Sensor() { std::cout << \"Sensor dtor\\n\"; }\n```\n\nNow `delete s` performs virtual dispatch, calls `~Lidar()` first (which can release `scan_buffer_`), and then calls `~Sensor()`. Output becomes:\n\n```\nLidar dtor\nSensor dtor\n```\n\n**Rule:** Any class with at least one virtual function must have a `virtual` destructor."
  },
  {
    "id": "02_classes_oop_raii_quiz_q04",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q4 — Operator Overloading: Member vs Free Function\n\nYou are implementing a `Pose2D` class with `x`, `y`, and `theta` fields.\n\n(a) Should `operator+=` be a member or a free function? Why?\n(b) Should `operator+` (binary addition) be a member or a free function? What advantage does the free form have?\n(c) Should `operator<<` (stream output) be a member or a free function? Why can't it be a member of `Pose2D`?",
    "back": "**(a) `operator+=` — member function.**\n\nThe left operand is always a `Pose2D`; no implicit conversion on the left side is needed. Compound assignment operators are conventionally members because they modify `*this` and return a reference to it.\n\n**(b) `operator+` — free function.**\n\nA free function allows implicit conversions on **both** sides of the `+`. If `operator+` were a member, `myPose + something` works (implicit conversion on the right), but `something + myPose` fails if `something` is not a `Pose2D`. As a free function, the compiler can apply conversions to either operand. The canonical pattern is to implement `+` in terms of `+=`:\n\n```cpp\nPose2D operator+(Pose2D lhs, const Pose2D& rhs) {\n    lhs += rhs;\n    return lhs;\n}\n```\n\n**(c) `operator<<` — free function.**\n\nThe left operand of `<<` is `std::ostream&`, which is not `Pose2D`. A member operator is called as `lhs.operator<<(rhs)`, meaning the left operand's type must be `Pose2D`. Since you cannot modify `std::ostream`, `operator<<` must be a free function (or a friend):\n\n```cpp\nstd::ostream& operator<<(std::ostream& os, const Pose2D& p) {\n    return os << \"Pose(\" << p.x << \", \" << p.y << \", \" << p.theta << \")\";\n}\n```"
  },
  {
    "id": "02_classes_oop_raii_quiz_q05",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q5 — Copy vs Move and `std::move`\n\nWhat does this program print? Explain why each line of output appears.\n\n```cpp\n#include <iostream>\n#include <string>\n#include <utility>\n\nclass Tag {\npublic:\n    Tag(const char* s) : s_{s} {}\n\n    Tag(const Tag& o)       : s_{o.s_}            { std::cout << \"copy \" << s_ << \"\\n\"; }\n    Tag(Tag&& o) noexcept   : s_{std::move(o.s_)} { std::cout << \"move \" << s_ << \"\\n\"; }\n\n    ~Tag() { std::cout << \"dtor \" << s_ << \"\\n\"; }\n\n    std::string s_;\n};\n\nTag make_tag() {\n    Tag t{\"sensor\"};\n    return t;\n}\n\nint main() {\n    Tag a{\"alpha\"};\n    Tag b = a;\n    Tag c = std::move(a);\n    Tag d = make_tag();\n}\n```",
    "back": "```\ncopy alpha\nmove alpha\nmove sensor       <- may be elided; see note\ndtor sensor\ndtor alpha\ndtor alpha\ndtor alpha\n```\n\n**Line by line:**\n\n1. `Tag a{\"alpha\"}` — calls the `const char*` converting constructor. No output (no copy/move).\n2. `Tag b = a` — `a` is an lvalue, so the **copy constructor** is called: prints `copy alpha`.\n3. `Tag c = std::move(a)` — `std::move(a)` casts `a` to an rvalue reference. The **move constructor** is selected: prints `move alpha`. After this, `a.s_` is in a valid but unspecified state (typically empty string).\n4. `Tag d = make_tag()` — the compiler is likely to apply **NRVO** (Named Return Value Optimisation), constructing `t` directly into `d`'s storage. If NRVO fires, there is **no copy or move** printed. If NRVO is suppressed (e.g., `-fno-elide-constructors`), the move constructor fires: `move sensor`.\n\n**Destructor order** (LIFO on scope exit): `d`, `c`, `b`, `a` — four `dtor` lines. `c.s_` is `\"alpha\"` (moved from), `a.s_` is likely `\"\"` after the move, but the destructor still runs on `a`.\n\n**Note:** Exact output for `d` depends on whether NRVO is applied. In practice with optimisations, no copy/move line appears for `make_tag()`."
  },
  {
    "id": "02_classes_oop_raii_quiz_q06",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q6 — Member Initializer List Order\n\nWhat does this program print? Explain the surprising result.\n\n```cpp\n#include <iostream>\n\nclass Range {\npublic:\n    Range(int low, int high)\n        : size_{high - low}   // MIL entry 1\n        , low_{low}           // MIL entry 2\n        , high_{high}         // MIL entry 3\n    {}\n\n    void print() const {\n        std::cout << \"low=\"  << low_\n                  << \" high=\" << high_\n                  << \" size=\" << size_ << \"\\n\";\n    }\n\nprivate:\n    int low_;     // declared first\n    int high_;    // declared second\n    int size_;    // declared third\n};\n\nint main() {\n    Range r{2, 10};\n    r.print();\n}\n```",
    "back": "```\nlow=2 high=10 size=8\n```\n\nWait — is this actually surprising? Let's look more carefully.\n\nMembers are initialized in **declaration order**: `low_`, then `high_`, then `size_`. The MIL lists them in a different order (`size_` first, then `low_`, then `high_`), but the initialization order follows the **declaration order** regardless.\n\nSo the actual initialization sequence is:\n1. `low_`  = `low` (= 2) ✓\n2. `high_` = `high` (= 10) ✓\n3. `size_` = `high - low` (= 8) ✓\n\nThis works correctly here because `size_` is initialized last (as declared) using the constructor parameters `high` and `low` — not using `high_` or `low_`. If `size_` were initialized using `high_ - low_` (the member variables rather than the parameters), and if `size_` were **declared before** `low_` and `high_`, then `size_` would use the uninitialized members and produce undefined behaviour.\n\n**The lesson:** The MIL order that matters is **declaration order**. Write the MIL in declaration order and use compiler flag `-Wreorder` to catch mismatches. Never initialize a member using another member that is declared after it."
  },
  {
    "id": "02_classes_oop_raii_quiz_q07",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q7 — Abstract Class and Slicing\n\nWhat does this program print, or does it fail to compile? Explain.\n\n```cpp\n#include <iostream>\n\nclass Shape {\npublic:\n    virtual double area() const = 0;\n    virtual ~Shape() = default;\n    void describe() const {\n        std::cout << \"area=\" << area() << \"\\n\";\n    }\n};\n\nclass Square : public Shape {\npublic:\n    explicit Square(double s) : side_{s} {}\n    double area() const override { return side_ * side_; }\nprivate:\n    double side_;\n};\n\nvoid print_area(Shape s) {   // takes Shape by VALUE\n    s.describe();\n}\n\nint main() {\n    Square sq{4.0};\n    print_area(sq);\n}\n```",
    "back": "**This does not compile.**\n\n`print_area` takes a `Shape` by value. To construct the parameter, the compiler must copy a `Square` into a `Shape` object. But `Shape` has a pure virtual function (`area() = 0`), making it an **abstract class**. Abstract classes cannot be instantiated — you cannot create an object of type `Shape`. The compiler rejects the copy.\n\nAdditionally, even if `Shape` were concrete, this would be the **slicing problem**: copying a `Square` into a `Shape` would discard `side_` and the vtable pointer for `Square`, leaving a useless truncated object.\n\n**Fix:** Pass by const reference or pointer to preserve polymorphism and avoid slicing:\n\n```cpp\nvoid print_area(const Shape& s) {\n    s.describe();   // virtual dispatch works correctly through reference\n}\n```"
  },
  {
    "id": "02_classes_oop_raii_quiz_q08",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q8 — Deep Copy Bug\n\nThe following class is supposed to manage a named array of doubles. Find all the bugs and explain how to fix each one.\n\n```cpp\nclass NamedArray {\npublic:\n    NamedArray(const char* name, std::size_t size)\n        : name_{name}, size_{size}, data_{new double[size]}\n    {}\n\n    ~NamedArray() { delete[] data_; }\n\n    double& operator[](std::size_t i) { return data_[i]; }\n\n    void print() const {\n        std::cout << name_ << \": \";\n        for (std::size_t i = 0; i < size_; ++i)\n            std::cout << data_[i] << \" \";\n        std::cout << \"\\n\";\n    }\n\nprivate:\n    const char* name_;\n    std::size_t size_;\n    double*     data_;\n};\n\nint main() {\n    NamedArray a{\"accel\", 3};\n    a[0] = 1.0; a[1] = 2.0; a[2] = 3.0;\n\n    NamedArray b = a;   // copy\n    b[0] = 99.0;\n\n    a.print();   // should print: accel: 1 2 3\n    b.print();   // should print: accel: 99 2 3\n}\n```",
    "back": "**Bug 1 — Shallow copy: missing copy constructor.**\n\nThe compiler-generated copy constructor copies `data_` by pointer value. After `NamedArray b = a`, both `a.data_` and `b.data_` point to the same heap array. `b[0] = 99.0` modifies both arrays. When either object is destroyed, `delete[] data_` runs. When the second is destroyed, it runs again on already-freed memory — **double-free, undefined behaviour**.\n\n**Fix:** Write a copy constructor that allocates a new array and copies the data:\n\n```cpp\nNamedArray(const NamedArray& o)\n    : name_{o.name_}, size_{o.size_}, data_{new double[o.size_]}\n{\n    std::copy(o.data_, o.data_ + o.size_, data_);\n}\n```\n\n**Bug 2 — Missing copy assignment operator.**\n\nSame problem applies to `b = a` (assignment after construction). Must implement copy assignment with a self-assignment guard.\n\n```cpp\nNamedArray& operator=(const NamedArray& o) {\n    if (this == &o) return *this;\n    double* tmp = new double[o.size_];\n    std::copy(o.data_, o.data_ + o.size_, tmp);\n    delete[] data_;\n    data_ = tmp;\n    size_ = o.size_;\n    name_ = o.name_;\n    return *this;\n}\n```\n\n**Bug 3 — `const char*` name ownership (minor).**\n\n`name_` stores a raw `const char*` pointing into a string literal or caller-owned buffer. If the caller's buffer goes out of scope, `name_` is dangling. Use `std::string` instead:\n\n```cpp\nstd::string name_;\n```\n\n**Summary:** This class violates the Rule of Three. Defining a destructor without also defining copy constructor and copy assignment operator is the bug. As a better long-term fix, replace the raw pointer with `std::vector<double>` and let it handle all memory management automatically."
  },
  {
    "id": "02_classes_oop_raii_quiz_q09",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q9 — `explicit` and Implicit Conversion Bug\n\nThe following code compiles and runs, but produces a silent logic bug. Identify it and fix it.\n\n```cpp\n#include <iostream>\n\nclass Radians {\npublic:\n    Radians(double r) : r_{r} {}\n    double value() const { return r_; }\nprivate:\n    double r_;\n};\n\nclass Degrees {\npublic:\n    Degrees(double d) : d_{d} {}\n    double value() const { return d_; }\nprivate:\n    double d_;\n};\n\nvoid set_heading(Radians heading) {\n    std::cout << \"heading set to \" << heading.value() << \" rad\\n\";\n}\n\nint main() {\n    Degrees user_input{90.0};\n    set_heading(user_input.value());   // pass the raw double\n}\n```",
    "back": "**The bug:** `user_input.value()` returns `90.0` (degrees). `set_heading` expects a `Radians`. The compiler silently converts `90.0` (a `double`) to `Radians{90.0}` using `Radians`'s non-`explicit` constructor. The function receives 90 radians — roughly 5160 degrees — instead of the intended π/2 radians. There is no warning.\n\n**Fix:** Mark `Radians(double)` as `explicit`. This makes the implicit conversion a compile error, forcing the caller to be deliberate:\n\n```cpp\nclass Radians {\npublic:\n    explicit Radians(double r) : r_{r} {}\n    ...\n};\n```\n\nNow `set_heading(user_input.value())` is a **compile error**. The programmer must write:\n\n```cpp\n// Option A: explicit construction\nset_heading(Radians{user_input.value() * M_PI / 180.0});\n\n// Option B: add a proper conversion function\nset_heading(Radians{degrees_to_radians(user_input.value())});\n```\n\n**Rule:** Whenever a single-argument constructor could silently accept a value from a semantically different type (degrees vs radians, metres vs feet, Hz vs rad/s), mark it `explicit`."
  },
  {
    "id": "02_classes_oop_raii_quiz_q10",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q10 — Static Member Definition Bug\n\nThis code compiles but fails to link. What is wrong and how do you fix it?\n\n```cpp\n// robot.hpp\n#pragma once\n#include <string>\n#include <iostream>\n\nclass Robot {\npublic:\n    explicit Robot(std::string name) : name_{std::move(name)} {\n        ++count_;\n    }\n    ~Robot() { --count_; }\n\n    static int count() { return count_; }\n\nprivate:\n    std::string name_;\n    static int count_;   // declaration only\n};\n\n// main.cpp\n#include \"robot.hpp\"\n\nint main() {\n    Robot r1{\"r2d2\"};\n    Robot r2{\"c3po\"};\n    std::cout << Robot::count() << \"\\n\";\n}\n```",
    "back": "**The bug:** `static int count_` inside the class body is a **declaration**, not a definition. The linker cannot find the storage for `count_` because it has never been defined. The error is typically:\n\n```\nundefined reference to `Robot::count_'\n```\n\n**Fix:** Add the definition in exactly one `.cpp` file (outside the class):\n\n```cpp\n// robot.cpp\n#include \"robot.hpp\"\n\nint Robot::count_ = 0;   // definition — allocates storage and sets initial value\n```\n\n**Alternative (C++17 and later):** Declare it `inline static` inside the class, which serves as both declaration and definition:\n\n```cpp\ninline static int count_ = 0;   // C++17 — no separate .cpp definition needed\n```\n\nOr use `static constexpr` for compile-time constants, which has also been inline-defined since C++17:\n\n```cpp\nstatic constexpr int kMaxRobots = 10;   // inline definition, no .cpp needed\n```"
  },
  {
    "id": "02_classes_oop_raii_quiz_q11",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q11 — Move Constructor and Move Assignment\n\nComplete the two missing special member functions for this class. The class owns a heap-allocated array. Your implementations must:\n- Transfer ownership without allocating new memory\n- Leave the source object in a valid, destructible state\n- Be marked `noexcept`\n\n```cpp\nclass FloatBuffer {\npublic:\n    explicit FloatBuffer(std::size_t n)\n        : size_{n}, data_{new float[n]}\n    {}\n\n    ~FloatBuffer() { delete[] data_; }\n\n    FloatBuffer(const FloatBuffer& o)\n        : size_{o.size_}, data_{new float[o.size_]}\n    {\n        std::copy(o.data_, o.data_ + o.size_, data_);\n    }\n\n    FloatBuffer& operator=(const FloatBuffer& o) {\n        if (this == &o) return *this;\n        float* tmp = new float[o.size_];\n        std::copy(o.data_, o.data_ + o.size_, tmp);\n        delete[] data_;\n        data_ = tmp;\n        size_ = o.size_;\n        return *this;\n    }\n\n    // TODO: move constructor\n    FloatBuffer(FloatBuffer&& o) noexcept\n        /* your implementation here */\n\n    // TODO: move assignment operator\n    FloatBuffer& operator=(FloatBuffer&& o) noexcept\n        /* your implementation here */\n\n    std::size_t size() const { return size_; }\n    float* data()            { return data_; }\n\nprivate:\n    std::size_t size_;\n    float*      data_;\n};\n```",
    "back": "```cpp\n// Move constructor — steal the source's resources, null it out.\nFloatBuffer(FloatBuffer&& o) noexcept\n    : size_{o.size_}, data_{o.data_}\n{\n    o.data_ = nullptr;   // source no longer owns the memory\n    o.size_ = 0;         // leave source in a valid, empty state\n}\n\n// Move assignment operator.\nFloatBuffer& operator=(FloatBuffer&& o) noexcept {\n    if (this == &o) return *this;   // self-move guard (rare but valid)\n\n    delete[] data_;       // release our current resource\n\n    data_   = o.data_;    // steal source's pointer\n    size_   = o.size_;\n\n    o.data_ = nullptr;    // null out source\n    o.size_ = 0;\n\n    return *this;\n}\n```\n\n**Key points:**\n- Setting `o.data_ = nullptr` is essential. When `o` is destroyed, its destructor calls `delete[] nullptr`, which is a no-op — safe.\n- `noexcept` is critical: `std::vector` uses move only when it is `noexcept`. Without it, reallocations fall back to copying.\n- The self-move guard (`this == &o`) is technically required by the standard (moved-from state must be valid), though self-move is uncommon in practice."
  },
  {
    "id": "02_classes_oop_raii_quiz_q12",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "quiz",
    "front": "### Q12 — Abstract Class Interface\n\nComplete the `LocalizationFilter` abstract base class and one concrete derived class. Fill in the three marked sections.\n\n```cpp\n#include <iostream>\n#include <string>\n#include <memory>\n#include <vector>\n\n// Abstract base: defines the interface for all state estimators.\nclass LocalizationFilter {\npublic:\n    // TODO 1: Declare a virtual destructor so derived classes\n    //         are destroyed correctly through a base pointer.\n\n\n    // TODO 2: Declare two pure virtual functions:\n    //   - predict(double dt)  — propagates state forward by dt seconds\n    //   - update(double meas) — incorporates a measurement\n    //   Both take a double and return void.\n\n\n    // Non-virtual utility: calls predict then update.\n    void step(double dt, double meas) {\n        predict(dt);\n        update(meas);\n    }\n\n    virtual std::string name() const = 0;\n};\n\n// TODO 3: Implement a concrete class KalmanFilter that:\n//   - inherits publicly from LocalizationFilter\n//   - has a constructor taking (double process_noise, double meas_noise)\n//   - overrides predict, update, and name\n//   - stores a double state_ (initialised to 0.0) and prints what it's doing\n\n\n// --- Driver (do not modify) ---\nint main() {\n    std::vector<std::unique_ptr<LocalizationFilter>> filters;\n    filters.push_back(std::make_unique<KalmanFilter>(0.1, 0.5));\n\n    for (auto& f : filters) {\n        f->step(0.05, 3.14);\n        std::cout << f->name() << \" state processed\\n\";\n    }\n}\n```",
    "back": "```cpp\n#include <iostream>\n#include <string>\n#include <memory>\n#include <vector>\n\nclass LocalizationFilter {\npublic:\n    // TODO 1: Virtual destructor.\n    virtual ~LocalizationFilter() = default;\n\n    // TODO 2: Pure virtual functions.\n    virtual void predict(double dt)    = 0;\n    virtual void update(double meas)   = 0;\n\n    void step(double dt, double meas) {\n        predict(dt);\n        update(meas);\n    }\n\n    virtual std::string name() const = 0;\n};\n\n// TODO 3: Concrete derived class.\nclass KalmanFilter : public LocalizationFilter {\npublic:\n    KalmanFilter(double process_noise, double meas_noise)\n        : process_noise_{process_noise}\n        , meas_noise_{meas_noise}\n        , state_{0.0}\n    {}\n\n    void predict(double dt) override {\n        // Simplified: just drift state by a tiny amount.\n        state_ += process_noise_ * dt;\n        std::cout << \"KalmanFilter predict dt=\" << dt\n                  << \" state=\" << state_ << \"\\n\";\n    }\n\n    void update(double meas) override {\n        // Simplified Kalman update: blend state toward measurement.\n        double gain = process_noise_ / (process_noise_ + meas_noise_);\n        state_ += gain * (meas - state_);\n        std::cout << \"KalmanFilter update meas=\" << meas\n                  << \" state=\" << state_ << \"\\n\";\n    }\n\n    std::string name() const override { return \"KalmanFilter\"; }\n\nprivate:\n    double process_noise_;\n    double meas_noise_;\n    double state_;\n};\n\nint main() {\n    std::vector<std::unique_ptr<LocalizationFilter>> filters;\n    filters.push_back(std::make_unique<KalmanFilter>(0.1, 0.5));\n\n    for (auto& f : filters) {\n        f->step(0.05, 3.14);\n        std::cout << f->name() << \" state processed\\n\";\n    }\n}\n```\n\n**What to notice:**\n- `virtual ~LocalizationFilter() = default` ensures that `delete` through a `LocalizationFilter*` calls `~KalmanFilter()` correctly.\n- `= 0` on `predict` and `update` makes `LocalizationFilter` abstract — you cannot instantiate it directly.\n- `override` on each derived method causes a compile error if the signature doesn't match a base virtual function.\n- The vector stores `unique_ptr<LocalizationFilter>` — polymorphism via pointer, no slicing."
  },
  {
    "id": "02_classes_oop_raii_concept_2_1_structs_vs_classes",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.1 Structs vs Classes",
    "back": "The only technical difference between `struct` and `class` in C++ is the **default access level**: `struct` members are `public` by default; `class` members are `private` by default. Everything else — constructors, destructors, inheritance, virtual functions — works identically in both.\n\nThe conventional distinction is semantic:\n\n| Use `struct` for | Use `class` for |\n|---|---|\n| Plain data aggregates (no invariants) | Types with invariants and behaviour |\n| POD-like types passed between subsystems | Objects that manage resources |\n| Simple message/record types | Encapsulated abstractions |\n\n```cpp\n#include <cstdint>\n#include <cmath>\n\n// struct: plain data, no invariants to protect.\n// Anyone can read/write any field freely.\nstruct Point2D {\n    double x;\n    double y;\n};\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_2_constructors_destructors",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.2 Constructors & Destructors",
    "back": "A **constructor** runs when an object is created; a **destructor** runs when it goes out of scope (or is deleted). Stack objects are destroyed in **LIFO order** — last constructed, first destroyed.\n\n### Default and Parameterized Constructors\n\n```cpp\n#include <iostream>\n#include <string>\n\nclass Sensor {\npublic:\n    // Default constructor — compiler generates one only if you define none.\n    // Once you write any constructor, the default is suppressed unless you\n    // explicitly request it.\n    Sensor() : name_{\"unnamed\"}, rate_hz_{10.0} {\n        std::cout << \"Sensor default ctor: \" << name_ << \"\\n\";\n    }\n\n    // Parameterized constructor.\n    Sensor(std::string name, double rate_hz)\n        : name_{std::move(name)}, rate_hz_{rate_hz}\n    {\n        std::cout << \"Sensor ctor: \" << name_ << \" @ \" << rate_hz_ << \" Hz\\n\";\n    }\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_3_member_initializer_lists",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.3 Member Initializer Lists",
    "back": "The **member initializer list** (MIL) runs before the constructor body. It is not just a style choice — for certain members it is the only option.\n\n### Why MIL Is Preferred\n\n```cpp\n#include <string>\n#include <iostream>\n\nclass Node {\npublic:\n    // WRONG: const member cannot be assigned in the body.\n    // Uncommenting the body-assignment version gives a compile error.\n    //\n    // Node(int id) {\n    //     id_ = id;     // error: assignment to const member\n    // }\n\n    // RIGHT: const and reference members MUST be in the MIL.\n    Node(int id, std::string name, double& shared_val)\n        : id_{id}                    // const int — only place to set it\n        , name_{std::move(name)}     // string: MIL calls move-ctor directly\n        , ref_{shared_val}           // reference — must bind here\n    {}\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_4_this_pointer_const_member_functions",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.4 `this` Pointer & const Member Functions",
    "back": "Inside a non-static member function, `this` is a pointer to the object the function was called on. Its type is `ClassName*` (or `const ClassName*` in a const member function).\n\n### When You Need `this` Explicitly\n\n```cpp\n#include <iostream>\n\nclass Filter {\npublic:\n    Filter(double cutoff, double gain)\n        : cutoff_{cutoff}, gain_{gain}\n    {}\n\n    // Name shadowing: parameter shadows member — use this-> to disambiguate.\n    void set_cutoff(double cutoff) {\n        this->cutoff_ = cutoff;   // this-> resolves ambiguity\n    }\n\n    // Better style: use trailing underscore for members (avoids the problem).\n    void set_gain(double gain) {\n        gain_ = gain;   // no ambiguity: gain vs gain_\n    }\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_5_copy_semantics",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.5 Copy Semantics",
    "back": "When you copy an object, C++ calls the **copy constructor** (initialization from another object) or **copy assignment operator** (assignment after both objects exist).\n\n### Shallow Copy and the Double-Free Bug\n\nIf your class owns heap memory through a raw pointer, the compiler-generated copy constructor copies the pointer value — both objects then point to the same allocation. Destroying either one leaves the other with a dangling pointer; destroying both is undefined behaviour (double-free).\n\n```cpp\n#include <cstring>\n#include <iostream>\n#include <stdexcept>\n\n// Dangerous: raw pointer without custom copy semantics.\nclass BadBuffer {\npublic:\n    explicit BadBuffer(std::size_t size)\n        : size_{size}, data_{new double[size]}\n    {\n        std::fill(data_, data_ + size_, 0.0);\n    }\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_6_move_semantics",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.6 Move Semantics",
    "back": "**Move semantics** let you transfer ownership of resources instead of copying them — critical for performance when returning large objects or inserting into containers.\n\n### lvalue vs rvalue Intuition\n\nAn **lvalue** has a name and a persistent address. An **rvalue** is a temporary with no name — it will cease to exist after the expression. The rule of thumb: *if you can take its address, it's an lvalue.*\n\n```cpp\nint x = 5;         // x is lvalue, 5 is rvalue\nstd::string s = std::string{\"hello\"};\n//           ^^ temporary rvalue\n```\n\n### Rvalue References and Move Operations\n\n```cpp\n#include <iostream>\n#include <cstring>\n#include <utility>  // std::move\n\nclass PointCloud {\npublic:\n    explicit PointCloud(std::size_t n)\n        : size_{n}, points_{new float[n * 3]}\n    {\n        std::cout << \"PointCloud ctor n=\" << n << \"\\n\";\n    }\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_7_raii_resource_acquisition_is_initial",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.7 RAII (Resource Acquisition Is Initialization)",
    "back": "RAII is the single most important C++ idiom. The rule: **acquire a resource in the constructor, release it in the destructor**. Because destructors run deterministically (scope exit, including exceptions), resource leaks become structurally impossible.\n\n### The Non-RAII Version (Fragile)\n\n```cpp\n// BAD: manual resource management — leaks on any early exit.\nvoid process_scan_bad(Lidar& lidar) {\n    lidar.activate();    // powers on, starts motor spinning\n\n    auto scan = lidar.get_scan();\n\n    if (scan.empty()) {\n        return;          // LEAK: lidar.deactivate() never called, motor keeps spinning\n    }\n\n    run_icp(scan);       // if this throws, deactivate() is also skipped\n\n    lidar.deactivate();  // only reached on the happy path\n}\n```\n\n### The RAII Version (Safe)\n\n```cpp\n#include <chrono>\n#include <iostream>\n#include <string>\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_8_smart_pointers",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.8 Smart Pointers",
    "back": "Smart pointers are RAII wrappers around raw pointers. They live in `<memory>`. Prefer them over `new`/`delete` in all new code.\n\n### `unique_ptr` — Sole Ownership\n\n```cpp\n#include <memory>\n#include <iostream>\n#include <string>\n\nclass Lidar {\npublic:\n    explicit Lidar(std::string model) : model_{std::move(model)} {\n        std::cout << \"Lidar \" << model_ << \" online\\n\";\n    }\n    ~Lidar() { std::cout << \"Lidar \" << model_ << \" offline\\n\"; }\n    void spin() { std::cout << model_ << \" spinning\\n\"; }\n\nprivate:\n    std::string model_;\n};\n\nvoid unique_ptr_demo() {\n    // make_unique is preferred over new: exception-safe, clear ownership.\n    auto lidar = std::make_unique<Lidar>(\"Velodyne VLP-16\");\n\n    lidar->spin();          // arrow operator: same as (*lidar).spin()\n    (*lidar).spin();        // dereference also works\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_9_operator_overloading",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.9 Operator Overloading",
    "back": "C++ lets you give user-defined types the same syntactic operators as built-in types. This makes math-heavy robotics code (Eigen-style vectors, transforms, poses) read naturally.\n\n### Member vs Free Function\n\n| Operator | Preferred form | Reason |\n|---|---|---|\n| `=`, `[]`, `()`, `->` | Member only | Language requires it |\n| `+=`, `-=`, `*=` | Member | Left operand is always this type |\n| `+`, `-`, `*` | Free function | Allows conversion on left operand |\n| `<<`, `>>` (stream) | Free function | Left operand is `ostream`, not your type |\n| `==`, `!=`, `<` | Free function | Symmetric treatment of both operands |\n\n```cpp\n#include <iostream>\n#include <cmath>\n#include <stdexcept>\n\nclass Vec2 {\npublic:\n    double x, y;\n\n    Vec2(double x = 0.0, double y = 0.0) : x{x}, y{y} {}\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_10_inheritance_virtual_functions",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.10 Inheritance & Virtual Functions",
    "back": "Consider a sensor fusion node that reads from wheel odometry, a GPS, and an IMU. Each sensor produces a pose estimate but they all work differently internally. Without inheritance you'd write separate code paths for each:\n\n```cpp\nvoid update(double dt) {\n    if (source_type == \"wheel\")  { update_wheel(dt); }\n    else if (source_type == \"gnss\") { update_gnss(dt); }\n    else if (source_type == \"imu\")  { update_imu(dt); }\n    // add a new sensor → edit this function\n}\n```\n\nEvery time you add a sensor you modify existing code. Fragile, and it grows without bound.\n\nInheritance solves this by letting you define a common interface (`LocalizationSource`) and have each sensor implement it. The fusion loop doesn't need to know which sensor it's talking to:\n\n```cpp\nfor (auto& source : sources) {\n    source->update(dt);   // correct implementation called automatically\n}\n```\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_2_11_static_members_methods",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: 2.11 static Members & Methods",
    "back": "A `static` member belongs to the **class**, not to any particular instance. All objects of the class share the same static data member.\n\n### Static Data Members\n\n```cpp\n#include <iostream>\n#include <string>\n#include <atomic>\n\nclass RosNode {\npublic:\n    explicit RosNode(std::string name) : name_{std::move(name)} {\n        ++instance_count_;   // increments the shared counter\n        std::cout << \"Node \" << name_ << \" created (total=\" << instance_count_ << \")\\n\";\n    }\n\n    ~RosNode() {\n        --instance_count_;\n        std::cout << \"Node \" << name_ << \" destroyed (total=\" << instance_count_ << \")\\n\";\n    }\n\n    // Static member function: no 'this' pointer, can only access static members.\n    static int instance_count() { return instance_count_; }\n\n    // Static constant — can be defined inline (integral or constexpr).\n    static constexpr int kMaxNodes = 16;\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "02_classes_oop_raii_concept_quick_reference",
    "module": "02_classes_oop_raii",
    "moduleName": "Classes Oop Raii",
    "type": "concept",
    "front": "Explain: Quick Reference",
    "back": "| Concept | Rule of thumb |\n|---|---|\n| struct vs class | struct = data, class = invariants |\n| explicit | Always on single-arg constructors unless implicit conversion is intended |\n| MIL order | Matches declaration order, not MIL order |\n| const member fn | Mark anything that doesn't mutate state as const |\n| Rule of Three | Destructor + copy ctor + copy assign — define all or none |\n| Rule of Five | + move ctor + move assign; mark both noexcept |\n| RAII | Resource in constructor, release in destructor |\n| Smart pointers | unique_ptr by default; shared_ptr only for genuinely shared ownership |\n| Virtual destructor | Required in any base class used polymorphically |\n| override | Always use on overriding functions |\n| Slicing | Store polymorphic objects through pointers/references, not values |\n| static members | Defined in .cpp; static fn has no this |"
  },
  {
    "id": "03_templates_generic_programming_quiz_q01",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q1 - Function Template Deduction\n\nGiven:\n\n```cpp\ntemplate <typename T>\nT max_value(T a, T b) {\n    return a < b ? b : a;\n}\n```\n\nWhy does `max_value(1, 2.5)` fail to compile, and what are two clean ways to call it?",
    "back": "Template argument deduction tries to infer one type `T` for both arguments. The first argument suggests `T = int`; the second suggests `T = double`. The compiler does not choose a common type during template deduction for this function, so deduction fails.\n\nTwo clean calls:\n\n```cpp\nauto a = max_value<double>(1, 2.5);     // explicitly choose T\nauto b = max_value(1.0, 2.5);           // make both arguments double\n```\n\nYou could also design a two-parameter template using `std::common_type_t`, but that is a different interface."
  },
  {
    "id": "03_templates_generic_programming_quiz_q02",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q2 - Templates vs Runtime Polymorphism\n\nWhen would you prefer a template-based sensor utility over a virtual base class? When would a virtual base class be the better choice?",
    "back": "Prefer templates when the type is known at compile time and you want zero-overhead, type-specific code: fixed-size math, typed buffers, compile-time traits, or algorithms over message structs.\n\nPrefer runtime polymorphism when you need to store different concrete types behind one interface at runtime, such as:\n\n```cpp\nstd::vector<std::unique_ptr<SensorPlugin>>\n```\n\nTemplates give compile-time polymorphism. Virtual functions give runtime polymorphism. They solve different problems."
  },
  {
    "id": "03_templates_generic_programming_quiz_q03",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q3 - Header-Only Templates\n\nWhy are template definitions usually placed in header files?",
    "back": "The compiler must see the full template definition at the point where it instantiates a concrete function or class. If another `.cpp` file only sees a declaration, it does not have enough information to generate `Template<T>` for the requested `T`.\n\nPutting template definitions in headers makes the definition visible to every translation unit. The alternative is explicit instantiation for every supported type in a `.cpp` file."
  },
  {
    "id": "03_templates_generic_programming_quiz_q04",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q4 - Non-Type Template Parameters\n\nWhy is `Vector<double, 3>` safer than a vector class that stores its dimension as a runtime integer?",
    "back": "The dimension is part of the type. A function that expects `Vector<double, 3>` cannot accidentally receive `Vector<double, 2>`. This catches dimension mismatch at compile time instead of producing a runtime error or, worse, a silent math bug.\n\nThis is useful for robotics math: 3D acceleration, 6D state vectors, 6x6 covariance matrices, and fixed-capacity embedded buffers."
  },
  {
    "id": "03_templates_generic_programming_quiz_q05",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q5 - Traits\n\nWhat is a traits class, and why is it useful in generic robotics code?",
    "back": "A traits class stores type-specific metadata or behavior outside the type itself.\n\n```cpp\ntemplate <typename Msg>\nstruct SensorTraits;\n```\n\nSpecializations can define facts such as default topic, expected rate, frame id, or whether a message has covariance. Generic code can then use `SensorTraits<Msg>` without hard-coding message-specific logic into the algorithm."
  },
  {
    "id": "03_templates_generic_programming_quiz_q06",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q6 - Partial Specialization\n\nCan function templates be partially specialized? What should you use instead?",
    "back": "Function templates cannot be partially specialized. Use overloads, constraints/concepts, or helper class templates that can be partially specialized.\n\nClass templates can be partially specialized:\n\n```cpp\ntemplate <typename T>\nstruct IsPointer { static constexpr bool value = false; };\n\ntemplate <typename T>\nstruct IsPointer<T*> { static constexpr bool value = true; };\n```"
  },
  {
    "id": "03_templates_generic_programming_quiz_q07",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q7 - `if constexpr`\n\nWhy can `if constexpr` compile code where a normal `if` would fail?",
    "back": "`if constexpr` evaluates the condition at compile time. The branch not selected is discarded before type checking for the instantiated template. A normal `if` requires both branches to compile, even if one branch will never execute at runtime.\n\nThis lets one template contain small type-dependent branches."
  },
  {
    "id": "03_templates_generic_programming_quiz_q08",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q8 - SFINAE vs Concepts\n\nWhat problem did SFINAE solve, and why are C++20 concepts usually clearer?",
    "back": "SFINAE lets invalid template substitutions remove an overload from consideration instead of producing a hard compiler error. It enabled constrained templates before C++20.\n\nConcepts express requirements directly and name them:\n\n```cpp\ntemplate <std::floating_point T>\nT reciprocal(T x);\n```\n\nDiagnostics are usually shorter and more directly tied to the failed requirement."
  },
  {
    "id": "03_templates_generic_programming_quiz_q09",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q9 - CRTP\n\nWhat does CRTP provide, and what is its main limitation compared with virtual functions?",
    "back": "CRTP provides compile-time polymorphism. The base class can call methods on the derived type using `static_cast<Derived&>(*this)`, avoiding virtual dispatch.\n\nIts main limitation is that it does not provide runtime polymorphism. You cannot use CRTP alone to store unrelated derived types in one homogeneous runtime collection through a base pointer."
  },
  {
    "id": "03_templates_generic_programming_quiz_q10",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q10 - Compile-Time Units\n\nWhy is this valuable?\n\n```cpp\nusing Meters = Quantity<MeterTag>;\nusing Seconds = Quantity<SecondTag>;\n```",
    "back": "Both types may store a `double`, but they are distinct at compile time. A function expecting `Meters` cannot accidentally receive `Seconds`. This prevents unit bugs without runtime overhead.\n\nThe larger design goal is to make invalid physical operations unrepresentable or at least explicit."
  },
  {
    "id": "03_templates_generic_programming_quiz_q11",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q11 - Constrained Timestamp Utility\n\nComplete `is_newer` so it only accepts types with a `timestamp_ns` member convertible to `std::uint64_t`.\n\n```cpp\n#include <concepts>\n#include <cstdint>\n\ntemplate <typename T>\nconcept HasTimestamp = /* TODO */;\n\ntemplate <HasTimestamp Msg>\nbool is_newer(const Msg& a, const Msg& b) {\n    /* TODO */\n}\n```",
    "back": "```cpp\n#include <concepts>\n#include <cstdint>\n\ntemplate <typename T>\nconcept HasTimestamp = requires(const T& msg) {\n    { msg.timestamp_ns } -> std::convertible_to<std::uint64_t>;\n};\n\ntemplate <HasTimestamp Msg>\nbool is_newer(const Msg& a, const Msg& b) {\n    return a.timestamp_ns > b.timestamp_ns;\n}\n```"
  },
  {
    "id": "03_templates_generic_programming_quiz_q12",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "quiz",
    "front": "### Q12 - Fixed-Size Vector Dot Product\n\nComplete the generic dot product.\n\n```cpp\ntemplate <typename T, std::size_t N>\nclass Vector {\npublic:\n    T& operator[](std::size_t i) { return data_[i]; }\n    const T& operator[](std::size_t i) const { return data_[i]; }\n\nprivate:\n    T data_[N]{};\n};\n\ntemplate <typename T, std::size_t N>\nT dot(const Vector<T, N>& a, const Vector<T, N>& b) {\n    /* TODO */\n}\n```",
    "back": "```cpp\ntemplate <typename T, std::size_t N>\nT dot(const Vector<T, N>& a, const Vector<T, N>& b) {\n    T sum{};\n    for (std::size_t i = 0; i < N; ++i) {\n        sum += a[i] * b[i];\n    }\n    return sum;\n}\n```\n\nThe dimension is part of the type, so `dot(Vector<double, 2>, Vector<double, 3>)` does not match this overload."
  },
  {
    "id": "03_templates_generic_programming_concept_3_1_why_generic_programming_matters",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.1 Why Generic Programming Matters",
    "back": "Robotics code repeats the same shapes everywhere:\n\n- Buffers of `ImuSample`, `LidarScan`, `Pose2d`, `WheelTick`\n- Math functions that should work for `float`, `double`, and sometimes fixed-point numeric types\n- Coordinate transforms over 2D and 3D vectors\n- Drivers that differ by message type but share queueing, timestamp validation, and diagnostics\n\nCopy-pasting the same implementation for each type is brittle. Runtime polymorphism solves some reuse problems, but it introduces indirection and often forces heap allocation. Templates solve a different problem: they let the compiler generate type-specific code from one source pattern.\n\n```cpp\ntemplate <typename T>\nT clamp(T value, T lo, T hi) {\n    if (value < lo) return lo;\n    if (hi < value) return hi;\n    return value;\n}\n\nint main() {\n    int pixels = clamp(300, 0, 255);\n    double yaw = clamp(4.2, -3.14159, 3.14159);\n}\n```\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "03_templates_generic_programming_concept_3_2_function_templates",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.2 Function Templates",
    "back": "A function template describes a family of functions.\n\n```cpp\ntemplate <typename T>\nT square(T x) {\n    return x * x;\n}\n\ndouble d = square(2.5);  // T = double\nint i = square(7);       // T = int\n```\n\nTemplate argument deduction uses function arguments, not the return type.\n\n```cpp\ntemplate <typename T>\nT make_zero() {\n    return T{};\n}\n\n// auto x = make_zero();      // error: T cannot be deduced\nauto y = make_zero<double>(); // explicit template argument\n```\n\nDeduction also preserves important distinctions:\n\n```cpp\ntemplate <typename T>\nvoid inspect(T value);       // passing by value drops top-level const and refs\n\ntemplate <typename T>\nvoid inspect_ref(const T& v); // keeps type without copying\n```\n\nFor robotics, prefer `const T&` for large message-like objects and plain `T` for small arithmetic values.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_3_class_templates",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.3 Class Templates",
    "back": "A class template describes a family of types.\n\n```cpp\ntemplate <typename T>\nclass RingBuffer {\npublic:\n    explicit RingBuffer(std::size_t capacity)\n        : capacity_{capacity}, data_{new T[capacity]}\n    {}\n\n    ~RingBuffer() { delete[] data_; }\n\n    RingBuffer(const RingBuffer&) = delete;\n    RingBuffer& operator=(const RingBuffer&) = delete;\n\nprivate:\n    std::size_t capacity_;\n    T* data_;\n};\n```\n\n`RingBuffer<ImuSample>` and `RingBuffer<double>` are distinct types. A function taking `RingBuffer<double>` cannot accidentally receive `RingBuffer<int>`.\n\nClass templates are the backbone of generic containers, filters, math types, and message adapters.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_4_non_type_template_parameters",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.4 Non-Type Template Parameters",
    "back": "Templates can take values as parameters, not just types.\n\n```cpp\ntemplate <typename T, std::size_t N>\nclass StaticVector {\npublic:\n    T& operator[](std::size_t i) { return data_[i]; }\n    const T& operator[](std::size_t i) const { return data_[i]; }\n    constexpr std::size_t size() const { return N; }\n\nprivate:\n    T data_[N]{};\n};\n\nStaticVector<double, 3> accel;\n```\n\nThe size is part of the type. This is useful when dimensions are fixed by physics:\n\n- `Vector<double, 3>` for acceleration\n- `Matrix<double, 6, 6>` for EKF covariance\n- `RingBuffer<ImuSample, 512>` for a fixed-size embedded queue\n\nCompile-time dimensions prevent entire classes of runtime mismatch bugs.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_5_template_instantiation_and_header_on",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.5 Template Instantiation and Header-Only Code",
    "back": "Templates are usually implemented in headers because the compiler must see the full definition at the point of instantiation.\n\n```cpp\n// math.hpp\ntemplate <typename T>\nT norm2(T x, T y) {\n    return x * x + y * y;\n}\n```\n\nIf you put a template definition only in a `.cpp` file, another translation unit cannot instantiate it unless you explicitly instantiate the needed types:\n\n```cpp\ntemplate double norm2<double>(double, double);\ntemplate float norm2<float>(float, float);\n```\n\nMost teaching code and many robotics utility libraries keep templates header-only for simplicity. Production libraries may use explicit instantiation to reduce compile times and binary size.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_6_specialization",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.6 Specialization",
    "back": "Specialization lets you customize a template for a specific type.\n\n```cpp\ntemplate <typename T>\nstruct SensorTraits {\n    static constexpr const char* topic = \"/unknown\";\n    static constexpr double default_rate_hz = 10.0;\n};\n\nstruct ImuSample {};\nstruct LidarScan {};\n\ntemplate <>\nstruct SensorTraits<ImuSample> {\n    static constexpr const char* topic = \"/imu/data\";\n    static constexpr double default_rate_hz = 100.0;\n};\n\ntemplate <>\nstruct SensorTraits<LidarScan> {\n    static constexpr const char* topic = \"/front_laser/scan\";\n    static constexpr double default_rate_hz = 20.0;\n};\n```\n\nTraits move type-specific facts out of generic algorithms:\n\n```cpp\ntemplate <typename Msg>\nvoid print_default_topic() {\n    std::cout << SensorTraits<Msg>::topic << \"\\n\";\n}\n```\n\nThis pattern appears in serialization libraries, ROS message adapters, Eigen traits, and numeric type systems.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_7_partial_specialization",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.7 Partial Specialization",
    "back": "Function templates cannot be partially specialized, but class templates can.\n\n```cpp\ntemplate <typename T>\nstruct IsPointer {\n    static constexpr bool value = false;\n};\n\ntemplate <typename T>\nstruct IsPointer<T*> {\n    static constexpr bool value = true;\n};\n```\n\n`IsPointer<int>::value` is false. `IsPointer<int*>::value` is true.\n\nPartial specialization is useful when behavior depends on a type shape: pointer, reference, array, optional, vector, or matrix expression.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_8_type_traits",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.8 Type Traits",
    "back": "The standard library provides type traits in `<type_traits>`.\n\n```cpp\n#include <type_traits>\n\ntemplate <typename T>\nvoid require_arithmetic(T value) {\n    static_assert(std::is_arithmetic_v<T>, \"T must be numeric\");\n    (void)value;\n}\n```\n\nTraits let you ask compile-time questions:\n\n- Is this type arithmetic?\n- Is it copy constructible?\n- Is it floating point?\n- Does it have a virtual destructor?\n- Can it be moved without throwing?\n\nGeneric robotics code often needs these questions. Example: a fixed-size vector should accept arithmetic scalar types but reject `std::string`.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_9_if_constexpr",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.9 `if constexpr`",
    "back": "`if constexpr` chooses a branch at compile time. The discarded branch does not need to compile for the current type.\n\n```cpp\n#include <type_traits>\n#include <cmath>\n\ntemplate <typename T>\ndouble magnitude(T x, T y) {\n    if constexpr (std::is_integral_v<T>) {\n        return std::sqrt(static_cast<double>(x * x + y * y));\n    } else {\n        return std::sqrt(x * x + y * y);\n    }\n}\n```\n\nThis is cleaner than SFINAE for many simple cases.\n\nUse `if constexpr` when one algorithm has a small type-dependent branch. Use specialization when the whole implementation meaningfully differs by type.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_10_sfinae",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.10 SFINAE",
    "back": "SFINAE means \"substitution failure is not an error.\" If template substitution fails while checking a candidate overload, the compiler removes that overload instead of producing an immediate error.\n\n```cpp\n#include <type_traits>\n\ntemplate <typename T,\n          typename = std::enable_if_t<std::is_floating_point_v<T>>>\nT reciprocal(T x) {\n    return T{1} / x;\n}\n```\n\nThis function only participates in overload resolution for floating-point types.\n\nSFINAE is powerful but hard to read. In modern C++, prefer concepts when available.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_11_c_20_concepts",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.11 C++20 Concepts",
    "back": "Concepts name compile-time requirements.\n\n```cpp\n#include <concepts>\n\ntemplate <typename T>\nconcept Scalar = std::integral<T> || std::floating_point<T>;\n\ntemplate <Scalar T>\nT saturate(T value, T lo, T hi) {\n    if (value < lo) return lo;\n    if (hi < value) return hi;\n    return value;\n}\n```\n\nConcepts improve diagnostics. Instead of pages of template errors, the compiler can say the type does not satisfy `Scalar`.\n\nYou can also define structural requirements:\n\n```cpp\ntemplate <typename T>\nconcept HasTimestamp = requires(const T& msg) {\n    { msg.timestamp_ns } -> std::convertible_to<std::uint64_t>;\n};\n\ntemplate <HasTimestamp Msg>\nbool is_newer(const Msg& a, const Msg& b) {\n    return a.timestamp_ns > b.timestamp_ns;\n}\n```\n\nThis is a strong fit for robotics message-processing utilities.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_12_crtp",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.12 CRTP",
    "back": "CRTP is the Curiously Recurring Template Pattern: a base class template takes the derived class as a template parameter.\n\n```cpp\ntemplate <typename Derived>\nclass Printable {\npublic:\n    void print() const {\n        static_cast<const Derived&>(*this).print_impl();\n    }\n};\n\nclass Pose2d : public Printable<Pose2d> {\npublic:\n    void print_impl() const {\n        std::cout << \"Pose2d\\n\";\n    }\n};\n```\n\nThis gives compile-time polymorphism. There is no virtual table and no dynamic dispatch.\n\nUse CRTP when:\n\n- The set of derived types is known at compile time\n- You need zero-overhead static dispatch\n- You want to share interface glue without runtime polymorphism\n\nDo not use CRTP when you need heterogeneous runtime collections like `std::vector<std::unique_ptr<SensorBase>>`. That is Module 2 runtime polymorphism territory.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_13_generic_physical_units",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.13 Generic Physical Units",
    "back": "Templates can make invalid physics unrepresentable.\n\n```cpp\ntemplate <typename Tag>\nclass Quantity {\npublic:\n    explicit constexpr Quantity(double value) : value_{value} {}\n    constexpr double value() const { return value_; }\n\nprivate:\n    double value_;\n};\n\nstruct MeterTag {};\nstruct SecondTag {};\n\nusing Meters = Quantity<MeterTag>;\nusing Seconds = Quantity<SecondTag>;\n```\n\n`Meters` and `Seconds` both store `double`, but they are different types. You cannot accidentally pass seconds where meters are expected unless you explicitly convert.\n\nThis pattern scales into dimensional analysis libraries, safer configuration parsers, and strongly typed navigation APIs.\n\n---"
  },
  {
    "id": "03_templates_generic_programming_concept_3_14_template_error_discipline",
    "module": "03_templates_generic_programming",
    "moduleName": "Templates Generic Programming",
    "type": "concept",
    "front": "Explain: 3.14 Template Error Discipline",
    "back": "Bad generic code produces unreadable compiler diagnostics. Good generic code fails near the call site with a clear reason.\n\nUse these tools:\n\n- `static_assert` for invariants that must always hold\n- Concepts for named requirements\n- Traits for type-specific metadata\n- Small templates with clear responsibilities\n- Tests that instantiate templates with more than one type\n\n```cpp\ntemplate <typename T>\nclass Covariance {\n    static_assert(std::is_floating_point_v<T>,\n                  \"Covariance<T> requires a floating-point scalar\");\n};\n```\n\n**Key takeaway:** generic programming is not about being clever. It is about capturing repeated structure once and making invalid uses fail at compile time."
  },
  {
    "id": "04_modern_cpp_quiz_q01",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q1 - `auto` and References\n\nGiven:\n\n```cpp\nconst std::string name = \"imu\";\nauto a = name;\nauto& b = name;\nconst auto& c = name;\n```\n\nWhat are the types of `a`, `b`, and `c`?",
    "back": "`a` is `std::string`. Plain `auto` drops top-level `const` and copies the value.\n\n`b` is `const std::string&`. Because `name` is const, a non-const reference cannot bind to it; `auto&` deduces the referenced type as const.\n\n`c` is `const std::string&`."
  },
  {
    "id": "04_modern_cpp_quiz_q02",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q2 - Structured Binding Copies\n\nWhy can this loop accidentally copy more than intended?\n\n```cpp\nfor (auto [id, scan] : scan_map) {\n    process(scan);\n}\n```\n\nHow should it be written if `scan` is large and read-only?",
    "back": "`auto [id, scan]` copies each key/value pair member into local variables. If `scan` owns a large vector, every iteration performs an expensive copy.\n\nUse a const reference binding:\n\n```cpp\nfor (const auto& [id, scan] : scan_map) {\n    process(scan);\n}\n```"
  },
  {
    "id": "04_modern_cpp_quiz_q03",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q3 - Lambda Captures\n\nWhy is `[&]` risky in a callback that may run later?",
    "back": "`[&]` captures all used local variables by reference. If the callback is stored and invoked after the surrounding function returns, those references dangle. This is common in asynchronous robotics code: timers, subscriptions, futures, and thread pools.\n\nPrefer explicit captures and make lifetime obvious. Capture values when possible; capture `this` only when object lifetime is guaranteed."
  },
  {
    "id": "04_modern_cpp_quiz_q04",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q4 - `std::function`\n\nWhat does `std::function<void(double)>` store, and what is the tradeoff?",
    "back": "It stores any callable that can be invoked with one `double` argument and returns `void`: a function pointer, lambda, functor, or bound member function.\n\nThe tradeoff is type-erased dispatch and possible allocation. It is useful at callback boundaries, but templates or concrete callable types are better for hot inner loops."
  },
  {
    "id": "04_modern_cpp_quiz_q05",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q5 - `std::optional`\n\nWhen is `std::optional<T>` a better return type than returning `T` with a sentinel value?",
    "back": "Use `optional` when absence is expected and recoverable. It makes \"no value\" explicit in the type system.\n\nSentinel values are fragile because they can overlap with valid values or be forgotten by callers."
  },
  {
    "id": "04_modern_cpp_quiz_q06",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q6 - `std::variant`\n\nWhen is `std::variant` a better fit than a virtual base class?",
    "back": "Use `variant` when the set of possible alternatives is fixed and known at compile time. It keeps storage value-based and makes handling cases explicit through `std::visit`.\n\nUse a virtual base class when alternatives need to be extended dynamically, loaded as plugins, or stored behind a stable runtime interface."
  },
  {
    "id": "04_modern_cpp_quiz_q07",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q7 - Overload Pattern\n\nWhat does this helper enable?\n\n```cpp\ntemplate <class... Ts>\nstruct Overload : Ts... {\n    using Ts::operator()...;\n};\ntemplate <class... Ts>\nOverload(Ts...) -> Overload<Ts...>;\n```",
    "back": "It combines multiple lambdas into one visitor object. Each lambda contributes an `operator()`. This is especially useful with `std::visit`."
  },
  {
    "id": "04_modern_cpp_quiz_q08",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q8 - `if constexpr`\n\nWhy does `if constexpr` help in generic code?",
    "back": "The condition is evaluated at compile time, and the branch not selected is discarded for the current instantiation. This allows type-dependent logic without requiring every branch to compile for every type."
  },
  {
    "id": "04_modern_cpp_quiz_q09",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q9 - Fold Expressions\n\nWhat does this return?\n\n```cpp\ntemplate <typename... Checks>\nbool all(Checks... checks) {\n    return (checks && ...);\n}\n```",
    "back": "It returns the logical AND of every argument in the parameter pack. For example, `all(true, true, false)` returns `false`.\n\nFor an empty pack, this unary fold over `&&` has identity `true`."
  },
  {
    "id": "04_modern_cpp_quiz_q10",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q10 - Config Parsing\n\nWhy should configuration strings be parsed at the boundary rather than passed deep into robotics subsystems?",
    "back": "Typed subsystems can enforce invariants early and avoid repeated string parsing. Passing strings deep into the system spreads error handling everywhere and allows invalid values to survive too long.\n\nParse once into types like `double`, `bool`, `std::optional`, or `std::variant`, validate, then pass a typed config object inward."
  },
  {
    "id": "04_modern_cpp_quiz_q11",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q11 - Optional Parser\n\nComplete `parse_double`.\n\n```cpp\n#include <optional>\n#include <string>\n\nstd::optional<double> parse_double(const std::string& text) {\n    // TODO\n}\n```\n\nIt should return `std::nullopt` if the full string is not a valid double.",
    "back": "```cpp\nstd::optional<double> parse_double(const std::string& text) {\n    try {\n        std::size_t pos = 0;\n        double value = std::stod(text, &pos);\n        if (pos != text.size()) {\n            return std::nullopt;\n        }\n        return value;\n    } catch (...) {\n        return std::nullopt;\n    }\n}\n```"
  },
  {
    "id": "04_modern_cpp_quiz_q12",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "quiz",
    "front": "### Q12 - Variant Visitor\n\nComplete `measurement_name`.\n\n```cpp\n#include <string>\n#include <variant>\n\nstruct Imu {};\nstruct Gps {};\nstruct Lidar {};\n\nusing Measurement = std::variant<Imu, Gps, Lidar>;\n\nstd::string measurement_name(const Measurement& m) {\n    // TODO\n}\n```",
    "back": "```cpp\ntemplate <class... Ts>\nstruct Overload : Ts... {\n    using Ts::operator()...;\n};\ntemplate <class... Ts>\nOverload(Ts...) -> Overload<Ts...>;\n\nstd::string measurement_name(const Measurement& m) {\n    return std::visit(Overload{\n        [](const Imu&) { return std::string{\"imu\"}; },\n        [](const Gps&) { return std::string{\"gps\"}; },\n        [](const Lidar&) { return std::string{\"lidar\"}; }\n    }, m);\n}\n```"
  },
  {
    "id": "04_modern_cpp_concept_4_1_what_modern_c_means",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.1 What \"Modern C++\" Means",
    "back": "Modern C++ is not a list of syntax tricks. It is a shift toward:\n\n- Expressing intent directly\n- Reducing manual type noise\n- Avoiding invalid sentinel values\n- Making ownership and absence explicit\n- Moving decisions from runtime to compile time when practical\n\nRobotics code benefits from this because failures often happen at subsystem boundaries: configuration files, sensor messages, state machines, and callback pipelines. Modern C++ gives you stronger vocabulary types for those boundaries.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_2_auto",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.2 `auto`",
    "back": "`auto` asks the compiler to deduce a variable type from its initializer.\n\n```cpp\nauto rate_hz = 50.0;     // double\nauto count = 10;         // int\nauto name = \"imu\";       // const char*\n```\n\nUse `auto` when the initializer already makes the type obvious or when the exact type is verbose:\n\n```cpp\nauto it = measurements.find(sensor_id);\nauto lock = std::scoped_lock{mutex};\n```\n\nDo not use `auto` when it hides important semantics:\n\n```cpp\nauto timeout = 1000; // milliseconds? seconds? message count?\n```\n\nIn that case, write the type or use a named duration:\n\n```cpp\nstd::chrono::milliseconds timeout{1000};\n```\n\nPlain `auto` drops references and top-level `const`.\n\n```cpp\nconst int value = 42;\nauto a = value;        // int\nconst auto b = value;  // const int\nauto& c = value;       // const int&\n```\n\nUse `const auto&` when iterating over large messages.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_3_structured_bindings",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.3 Structured Bindings",
    "back": "Structured bindings unpack tuple-like or aggregate values.\n\n```cpp\nstruct Pose2d {\n    double x;\n    double y;\n    double theta;\n};\n\nPose2d pose{1.0, 2.0, 0.5};\nauto [x, y, theta] = pose;\n```\n\nThey are especially useful with maps:\n\n```cpp\nfor (const auto& [name, rate_hz] : sensor_rates) {\n    std::cout << name << \" runs at \" << rate_hz << \" Hz\\n\";\n}\n```\n\nBe intentional about copies:\n\n```cpp\nauto [k, v] = *it;        // copies key and value\nauto& [rk, rv] = *it;     // references pair members\nconst auto& [ck, cv] = *it;\n```\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_4_lambdas",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.4 Lambdas",
    "back": "A lambda creates a small function object.\n\n```cpp\nauto is_recent = [now](const Measurement& m) {\n    return now - m.timestamp_ns < 100000000;\n};\n```\n\nCapture deliberately:\n\n- `[x]` captures `x` by value\n- `[&x]` captures `x` by reference\n- `[=]` captures used variables by value\n- `[&]` captures used variables by reference\n\nAvoid broad captures in long-lived callbacks. In robotics, callbacks may outlive the scope you had in mind.\n\n```cpp\nauto callback = [this](const ImuMsg& msg) {\n    filter_.update(msg);\n};\n```\n\nThis is only safe if `this` is guaranteed to outlive the callback registration.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_5_generic_lambdas",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.5 Generic Lambdas",
    "back": "Generic lambdas use `auto` parameters.\n\n```cpp\nauto print_timestamp = [](const auto& msg) {\n    std::cout << msg.timestamp_ns << \"\\n\";\n};\n```\n\nThis is shorthand for a templated function object. It is useful for small local algorithms, not for public interfaces where named concepts and clearer diagnostics matter.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_6_algorithms_over_hand_written_loops",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.6 Algorithms Over Hand-Written Loops",
    "back": "The standard algorithms make intent explicit.\n\n```cpp\nauto valid = std::all_of(samples.begin(), samples.end(),\n                         [](const auto& s) { return s.range_m >= 0.0; });\n```\n\nCommon algorithms:\n\n- `std::find_if`\n- `std::count_if`\n- `std::transform`\n- `std::accumulate`\n- `std::all_of`, `std::any_of`, `std::none_of`\n- `std::sort`\n\nUse algorithms when they clarify intent. Use explicit loops when the control flow is genuinely more readable.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_7_std_function",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.7 `std::function`",
    "back": "`std::function` stores any callable with a matching signature.\n\n```cpp\nstd::function<void(double)> update_callback;\n\nupdate_callback = [](double dt) {\n    std::cout << \"dt=\" << dt << \"\\n\";\n};\n```\n\nIt is useful for callback registration and plugin-like seams:\n\n```cpp\nclass Timer {\npublic:\n    using Callback = std::function<void(double)>;\n    explicit Timer(Callback cb) : callback_{std::move(cb)} {}\n\n    void tick(double dt) { callback_(dt); }\n\nprivate:\n    Callback callback_;\n};\n```\n\nTradeoff: `std::function` may allocate and performs type-erased dispatch. For hot inner loops, prefer templates or direct function objects.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_8_std_optional",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.8 `std::optional`",
    "back": "`std::optional<T>` represents either a `T` or no value.\n\n```cpp\nstd::optional<double> parse_double(const std::string& text);\n```\n\nThis is better than returning magic sentinel values like `-1.0` or `NaN` when absence is expected and not exceptional.\n\n```cpp\nauto maybe_rate = parse_double(config[\"rate_hz\"]);\nif (!maybe_rate) {\n    std::cout << \"rate missing or invalid\\n\";\n} else {\n    std::cout << \"rate=\" << *maybe_rate << \"\\n\";\n}\n```\n\nUse `optional` for recoverable absence. Use exceptions or error objects for failures that require explanation.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_9_std_variant",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.9 `std::variant`",
    "back": "`std::variant` stores one value from a fixed set of alternatives.\n\n```cpp\nstruct ImuMeasurement { double ax, ay, az; };\nstruct GpsMeasurement { double lat, lon, alt; };\n\nusing Measurement = std::variant<ImuMeasurement, GpsMeasurement>;\n```\n\nVisit a variant with `std::visit`:\n\n```cpp\nstd::visit([](const auto& msg) {\n    std::cout << \"received measurement\\n\";\n}, measurement);\n```\n\nUse variants when all alternatives are known and closed. Use runtime polymorphism when new alternatives must be added through plugins or dynamic loading.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_10_overload_pattern_for_variant_visito",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.10 Overload Pattern for Variant Visitors",
    "back": "The overload pattern combines multiple lambdas into one visitor.\n\n```cpp\ntemplate <class... Ts>\nstruct Overload : Ts... {\n    using Ts::operator()...;\n};\n\ntemplate <class... Ts>\nOverload(Ts...) -> Overload<Ts...>;\n```\n\nUsage:\n\n```cpp\nstd::visit(Overload{\n    [](const ImuMeasurement& imu) { handle_imu(imu); },\n    [](const GpsMeasurement& gps) { handle_gps(gps); }\n}, measurement);\n```\n\nThis is a compact way to write type-specific logic while keeping the set of cases explicit.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_11_if_constexpr",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.11 `if constexpr`",
    "back": "`if constexpr` was introduced in Module 3 for templates. In modern C++, it often replaces complicated overload sets for small compile-time decisions.\n\n```cpp\ntemplate <typename Msg>\nstd::string frame_id_for() {\n    if constexpr (std::is_same_v<Msg, ImuMeasurement>) {\n        return \"imu_link\";\n    } else if constexpr (std::is_same_v<Msg, GpsMeasurement>) {\n        return \"gps_link\";\n    } else {\n        return \"base_link\";\n    }\n}\n```\n\nThe discarded branches are not instantiated for the current type.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_12_fold_expressions",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.12 Fold Expressions",
    "back": "Fold expressions reduce a parameter pack.\n\n```cpp\ntemplate <typename... Values>\nauto sum(Values... values) {\n    return (values + ...);\n}\n```\n\nThey are useful for compact validation:\n\n```cpp\ntemplate <typename... Checks>\nbool all(Checks... checks) {\n    return (checks && ...);\n}\n```\n\nFor a robotics configuration object:\n\n```cpp\nbool ok = all(rate_hz > 0.0, timeout_ms > 0, frame_id != \"\");\n```\n\nFold expressions are powerful, but do not use them to hide complex logic.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_13_config_parsing_with_vocabulary_type",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.13 Config Parsing with Vocabulary Types",
    "back": "Configuration is a common source of robotics bugs. Modern C++ gives better tools:\n\n- `std::optional<T>` for missing or invalid values\n- `std::variant` for typed config values\n- lambdas for local parsers\n- structured bindings for key/value iteration\n\n```cpp\nusing ConfigValue = std::variant<int, double, std::string, bool>;\n\nstruct ControllerConfig {\n    double rate_hz;\n    std::string frame_id;\n    bool publish_debug;\n};\n```\n\nA parser should separate lexing text into keys and values, converting values into typed fields, and validating invariants.\n\nDo not let every subsystem parse strings independently. Parse once at the boundary and pass typed configuration inward.\n\n---"
  },
  {
    "id": "04_modern_cpp_concept_4_14_small_state_machines",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.14 Small State Machines",
    "back": "State machines appear in robot behavior, sensor drivers, lifecycle nodes, and recovery logic.\n\n```cpp\nenum class State {\n    Idle,\n    Armed,\n    Running,\n    Fault\n};\n\nenum class Event {\n    Configure,\n    Start,\n    Stop,\n    Error\n};\n```\n\nA transition function makes behavior explicit:\n\n```cpp\nconstexpr State transition(State s, Event e) {\n    switch (s) {\n    case State::Idle:\n        return e == Event::Configure ? State::Armed : s;\n    case State::Armed:\n        return e == Event::Start ? State::Running : s;\n    case State::Running:\n        return e == Event::Stop ? State::Armed : s;\n    case State::Fault:\n        return State::Fault;\n    }\n    return State::Fault;\n}\n```\n\nIf the transition function is `constexpr`, you can test behavior at compile time:\n\n```cpp\nstatic_assert(transition(State::Idle, Event::Configure) == State::Armed);\n```\n\n*(see concepts.md for full section)*"
  },
  {
    "id": "04_modern_cpp_concept_4_15_practical_rules",
    "module": "04_modern_cpp",
    "moduleName": "Modern Cpp",
    "type": "concept",
    "front": "Explain: 4.15 Practical Rules",
    "back": "- Use `auto` when it improves readability, not to avoid thinking about types.\n- Prefer `const auto&` for large objects in loops.\n- Use structured bindings for tuple-like return values and map iteration.\n- Use lambdas for local behavior; use named functions when behavior is reused or tested separately.\n- Use `optional` instead of sentinel values for expected absence.\n- Use `variant` when the set of alternatives is closed and known.\n- Use `std::function` at callback boundaries, not in tight numeric loops.\n- Use `constexpr` and `static_assert` for small rules that should be proven at compile time.\n\n**Key takeaway:** Modern C++ is useful when it makes constraints and intent more visible. If it makes the code clever but harder to audit, it is the wrong tool."
  },
  {
    "id": "05_standard_library_quiz_q01",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "quiz",
    "front": "# Module 5 Quiz - Standard Library\n\n## Q1 - Container Choice\n\nWhen should you prefer `std::deque` over `std::vector`?",
    "back": "Use `std::deque` when you need efficient insertion/removal at both ends, such as a sliding time window. `std::vector` is better for contiguous storage and random-access numeric processing."
  },
  {
    "id": "05_standard_library_quiz_q02",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "quiz",
    "front": "Why is `std::remove_if` alone not enough to delete elements from a vector?",
    "back": "`remove_if` only moves kept elements toward the front and returns the new logical end. The vector size is unchanged. You must call `erase(new_end, end)` to shrink it."
  },
  {
    "id": "05_standard_library_quiz_q03",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "quiz",
    "front": "Why is `std::chrono::steady_clock` better than system time for measuring elapsed duration?",
    "back": "`steady_clock` is monotonic. System time can move backward or forward due to NTP/manual changes, which can break timeout and duration calculations."
  },
  {
    "id": "05_standard_library_quiz_q04",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "quiz",
    "front": "What is the main lifetime risk of `std::string_view`?",
    "back": "It does not own the characters. If the referenced string is destroyed or modified incompatibly, the view dangles. Use it for parameters, not long-term storage unless lifetime is guaranteed."
  },
  {
    "id": "05_standard_library_quiz_q05",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "quiz",
    "front": "What problem does `vector.reserve(n)` solve?",
    "back": "It preallocates capacity so repeated `push_back` calls avoid multiple reallocations and element moves/copies."
  },
  {
    "id": "05_standard_library_concept_5_1_why_the_stl_matters",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.1 Why the STL Matters",
    "back": "Robotics systems move data through queues, maps, windows, transforms, and filters. The standard library already provides well-tested building blocks for these jobs. Prefer STL containers and algorithms until measurement proves they are not enough.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_2_containers",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.2 Containers",
    "back": "Use `std::vector` for contiguous dynamic arrays and numeric data. Use `std::deque` when pushing and popping at both ends. Use `std::array` for fixed-size stack storage. Use `std::map` or `std::unordered_map` for key/value lookup.\n\n```cpp\nstd::vector<double> ranges;\nstd::array<double, 3> accel{};\nstd::deque<ImuSample> imu_window;\nstd::unordered_map<std::string, double> rates;\n```\n\nContainer choice is an engineering decision about access pattern, allocation, ordering, and invalidation.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_3_iterators",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.3 Iterators",
    "back": "Iterators generalize positions in a sequence. Algorithms operate on iterator ranges:\n\n```cpp\nstd::sort(values.begin(), values.end());\nauto it = std::find(values.begin(), values.end(), 42);\n```\n\nThe half-open range `[begin, end)` is standard: begin is included, end is one past the last element.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_4_algorithms",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.4 Algorithms",
    "back": "Algorithms express intent:\n\n- `std::find_if` for search\n- `std::count_if` for counting matches\n- `std::transform` for mapping values\n- `std::accumulate` for reductions\n- `std::remove_if` plus `erase` for filtering containers\n- `std::lower_bound` for binary search over sorted ranges\n\nPrefer algorithms when they make the code easier to audit.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_5_erase_remove",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.5 Erase-Remove",
    "back": "`std::remove_if` rearranges elements and returns the new logical end. It does not shrink the container. Pair it with `erase`:\n\n```cpp\nvalues.erase(std::remove_if(values.begin(), values.end(), pred), values.end());\n```\n\nThis pattern appears often when dropping invalid sensor readings.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_6_std_chrono",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.6 `std::chrono`",
    "back": "Use `std::chrono` instead of raw integers for time.\n\n```cpp\nusing Clock = std::chrono::steady_clock;\nauto start = Clock::now();\nstd::chrono::milliseconds timeout{100};\n```\n\n`steady_clock` is monotonic and appropriate for measuring durations. System time can jump and is not appropriate for elapsed-time logic.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_7_std_string_view",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.7 `std::string_view`",
    "back": "`std::string_view` is a non-owning view into character data. It avoids copies but does not extend lifetime.\n\n```cpp\nvoid parse_key(std::string_view key);\n```\n\nDo not store a `string_view` to a temporary string. Store `std::string` when ownership is needed.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_8_custom_allocators",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.8 Custom Allocators",
    "back": "Allocators control how containers acquire memory. Most robotics code should not write custom allocators early. First use `reserve`, fixed-capacity containers, and clear ownership boundaries. Allocators matter in real-time systems when dynamic allocation must be bounded or eliminated.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_9_time_based_windows",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.9 Time-Based Windows",
    "back": "A time window stores recent samples and evicts old ones:\n\n```cpp\nwhile (!samples.empty() && now - samples.front().stamp > window) {\n    samples.pop_front();\n}\n```\n\nUse `std::deque` for efficient front removal and back insertion.\n\n---"
  },
  {
    "id": "05_standard_library_concept_5_10_practical_rules",
    "module": "05_standard_library",
    "moduleName": "Standard Library",
    "type": "concept",
    "front": "Explain: 5.10 Practical Rules",
    "back": "- Default to `std::vector` unless another access pattern is clearly better.\n- Call `reserve` when the approximate size is known.\n- Use `std::array` for fixed dimensions.\n- Use `std::chrono` for durations and deadlines.\n- Use `std::string_view` only for non-owning parameters.\n- Prefer algorithms for search, transform, reduction, and filtering."
  },
  {
    "id": "06_concurrency_quiz_q01",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "quiz",
    "front": "# Module 6 Quiz - Concurrency\n\n## Q1 - Joinable Threads\n\nWhat happens if a joinable `std::thread` is destroyed?",
    "back": "The program calls `std::terminate`. You must `join()` or `detach()` before destruction. In most robotics code, prefer `join()` for controlled shutdown."
  },
  {
    "id": "06_concurrency_quiz_q02",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "quiz",
    "front": "Why should `cv.wait(lock, predicate)` be preferred over plain `cv.wait(lock)`?",
    "back": "Threads can wake spuriously, and notifications can happen before the waiter starts waiting. The predicate rechecks the actual condition under the lock."
  },
  {
    "id": "06_concurrency_quiz_q03",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "quiz",
    "front": "Why is `std::atomic<bool>` not enough to protect a queue?",
    "back": "The queue has compound state: storage, size, head/tail, and invariants across those fields. A mutex is needed to protect those multi-step updates."
  },
  {
    "id": "06_concurrency_quiz_q04",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "quiz",
    "front": "Why should callbacks not usually be invoked while holding a mutex?",
    "back": "Callbacks may call back into your object, block, throw, or acquire other locks. Holding your mutex during callback execution increases deadlock and latency risk."
  },
  {
    "id": "06_concurrency_quiz_q05",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "quiz",
    "front": "Why should a bounded queue define its overload behavior explicitly?",
    "back": "Robotics systems need predictable behavior under load. The queue should clearly block, reject new items, drop oldest, or drop newest rather than growing unbounded and failing later."
  },
  {
    "id": "06_concurrency_concept_6_1_why_concurrency_matters",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.1 Why Concurrency Matters",
    "back": "Robots run many activities at once: sensor acquisition, filtering, mapping, planning, logging, diagnostics, and control. C++ gives low-level concurrency primitives, but correctness depends on disciplined ownership and synchronization.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_2_threads",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.2 Threads",
    "back": "`std::thread` starts a new execution thread. A joinable thread must be joined or detached before destruction.\n\n```cpp\nstd::thread worker{[] { do_work(); }};\nworker.join();\n```\n\nPrefer joining. Detached threads are hard to reason about and dangerous during shutdown.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_3_mutexes_and_locks",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.3 Mutexes and Locks",
    "back": "Use `std::mutex` to protect shared data. Use RAII locks, not manual lock/unlock.\n\n```cpp\nstd::mutex m;\nstd::vector<int> data;\n\n{\n    std::lock_guard<std::mutex> lock{m};\n    data.push_back(1);\n}\n```\n\nKeep critical sections short. Do not hold locks while calling unknown callbacks.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_4_condition_variables",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.4 Condition Variables",
    "back": "`std::condition_variable` lets a thread sleep until data is available.\n\n```cpp\ncv.wait(lock, [&] { return ready; });\n```\n\nAlways wait with a predicate. Condition variables can wake spuriously.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_5_atomics",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.5 Atomics",
    "back": "Use `std::atomic<T>` for simple shared flags and counters.\n\n```cpp\nstd::atomic<bool> stop{false};\n```\n\nAtomics do not make compound data structures safe. Use a mutex for multi-field invariants.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_6_futures",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.6 Futures",
    "back": "`std::async` and `std::future` are useful for one-shot asynchronous results.\n\n```cpp\nauto result = std::async(std::launch::async, compute_map);\nMap map = result.get();\n```\n\nFor long-running pipelines, explicit threads and queues are usually clearer.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_7_producer_consumer_queues",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.7 Producer/Consumer Queues",
    "back": "A common robotics pattern is one thread producing sensor messages and another consuming them.\n\nThe queue needs:\n\n- A mutex to protect storage\n- A condition variable to block consumers\n- A shutdown flag\n- A capacity policy for overload\n\nFor real-time systems, prefer bounded queues and explicit drop behavior.\n\n---"
  },
  {
    "id": "06_concurrency_concept_6_8_practical_rules",
    "module": "06_concurrency",
    "moduleName": "Concurrency",
    "type": "concept",
    "front": "Explain: 6.8 Practical Rules",
    "back": "- Do not share mutable data without synchronization.\n- Use RAII locks.\n- Wait on condition variables with predicates.\n- Use atomics for simple flags only.\n- Define shutdown behavior early.\n- Avoid holding locks while executing callbacks."
  },
  {
    "id": "07_performance_tooling_quiz_q01",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "quiz",
    "front": "# Module 7 Quiz - Performance & Tooling\n\n## Q1 - Debug vs Release\n\nWhy should benchmarks use Release builds?",
    "back": "Debug builds disable or reduce optimization and include extra debug information. They can be orders of magnitude slower and do not represent production performance."
  },
  {
    "id": "07_performance_tooling_quiz_q02",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "quiz",
    "front": "How can `reserve` reduce latency jitter?",
    "back": "It preallocates vector capacity and avoids repeated reallocations during `push_back`, reducing unpredictable allocation and copy/move costs."
  },
  {
    "id": "07_performance_tooling_quiz_q03",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "quiz",
    "front": "Why is `std::vector` usually better than `std::list` for point clouds?",
    "back": "Point clouds are traversed sequentially. `std::vector` stores elements contiguously, which is cache-friendly. `std::list` scatters nodes across memory and adds pointer chasing."
  },
  {
    "id": "07_performance_tooling_quiz_q04",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "quiz",
    "front": "Which sanitizer would you use for data races?",
    "back": "ThreadSanitizer."
  },
  {
    "id": "07_performance_tooling_quiz_q05",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "quiz",
    "front": "Why is a single timing run weak evidence?",
    "back": "Timing varies due to scheduler noise, CPU frequency changes, cache state, and background processes. Use repeated runs and compare medians or distributions."
  },
  {
    "id": "07_performance_tooling_concept_7_1_measure_first",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.1 Measure First",
    "back": "Performance work without measurement is guessing. Start with a clear workload, a baseline, and repeatable timing.\n\nUse `std::chrono::steady_clock` for simple local measurements. Use profilers such as `perf`, `callgrind`, or tracing tools for real systems.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_2_big_o_is_not_enough",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.2 Big-O Is Not Enough",
    "back": "Two `O(n)` algorithms can differ massively because of cache locality, allocation, branching, and vectorization. Robotics pipelines often process large arrays of points, scans, or grid cells, where memory access dominates.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_3_allocation_discipline",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.3 Allocation Discipline",
    "back": "Repeated allocation in hot paths causes jitter. Use:\n\n- `reserve` for vectors\n- object reuse for frame buffers\n- fixed-size storage where dimensions are known\n- bounded queues for real-time pipelines\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_4_cache_locality",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.4 Cache Locality",
    "back": "Contiguous memory is faster to traverse than scattered allocations. Prefer `std::vector<Point>` over `std::list<Point>` for point clouds and scans.\n\nStructure-of-arrays can outperform array-of-structures for SIMD-heavy numeric kernels, but it complicates APIs. Measure before changing representation.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_5_simd_basics",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.5 SIMD Basics",
    "back": "SIMD executes the same operation over multiple values. Compilers can auto-vectorize simple loops if data is contiguous and dependencies are clear. Write simple loops first; inspect compiler output only when performance matters.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_6_sanitizers",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.6 Sanitizers",
    "back": "Sanitizers catch bugs early:\n\n- AddressSanitizer: memory errors\n- UndefinedBehaviorSanitizer: undefined behavior\n- ThreadSanitizer: data races\n\nThey are not production runtime dependencies; they are development/test tools.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_7_debuggers_and_profilers",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.7 Debuggers and Profilers",
    "back": "Use GDB for correctness bugs and `perf` for CPU profiling. Logging can perturb timing and hide races. Prefer targeted instrumentation.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_8_cmake_build_types",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.8 CMake Build Types",
    "back": "Debug builds are for debugging. Release builds are for performance measurement.\n\n```bash\ncmake -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build\n```\n\nDo not compare performance numbers from Debug builds.\n\n---"
  },
  {
    "id": "07_performance_tooling_concept_7_9_practical_rules",
    "module": "07_performance_tooling",
    "moduleName": "Performance Tooling",
    "type": "concept",
    "front": "Explain: 7.9 Practical Rules",
    "back": "- Measure before optimizing.\n- Benchmark Release builds.\n- Avoid allocations in hot callbacks.\n- Prefer contiguous storage.\n- Keep numeric loops simple.\n- Use sanitizers before profiling."
  },
  {
    "id": "08_robotics_patterns_quiz_q01",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "quiz",
    "front": "# Module 8 Quiz - Robotics Patterns\n\n## Q1 - Observer Pattern\n\nWhat is the primary risk when observers are callbacks?",
    "back": "Lifetime and reentrancy. A callback may refer to destroyed objects, call back into the publisher, block, or throw. Avoid holding locks while invoking observers."
  },
  {
    "id": "08_robotics_patterns_quiz_q02",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "quiz",
    "front": "Why use a factory for localization filters?",
    "back": "It centralizes construction and lets the rest of the system depend on an interface rather than concrete filter classes. Configuration can select implementations at runtime."
  },
  {
    "id": "08_robotics_patterns_quiz_q03",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "quiz",
    "front": "Why prefer `enum class` over plain `enum` for states?",
    "back": "`enum class` is scoped and does not implicitly convert to integers. This reduces accidental comparisons and namespace pollution."
  },
  {
    "id": "08_robotics_patterns_quiz_q04",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "quiz",
    "front": "What problem does entity-component style solve?",
    "back": "It allows optional sets of data to be attached to many entities without forcing a deep inheritance hierarchy."
  },
  {
    "id": "08_robotics_patterns_quiz_q05",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "quiz",
    "front": "When should you not introduce a pattern?",
    "back": "When the simpler design is clearer and the expected extension pressure is speculative. Patterns should reduce complexity under real constraints, not add abstraction for its own sake."
  },
  {
    "id": "08_robotics_patterns_concept_8_1_patterns_are_tradeoffs",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.1 Patterns Are Tradeoffs",
    "back": "Patterns are not decorations. A pattern is useful when it makes ownership, extension points, data flow, or invariants clearer. Robotics code should prefer simple designs until the pressure for a pattern is real.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_2_eigen_and_linear_algebra_boundaries",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.2 Eigen and Linear Algebra Boundaries",
    "back": "Production robotics often uses Eigen for vectors, matrices, transforms, and decompositions. Keep math types near math-heavy code and avoid leaking implementation details through every API. Even without Eigen, the boundary idea matters: pass typed poses, states, and covariances rather than raw arrays.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_3_observer_pattern",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.3 Observer Pattern",
    "back": "The observer pattern broadcasts events to subscribers:\n\n```cpp\nclass SensorBus {\npublic:\n    using Callback = std::function<void(const ImuSample&)>;\n    void subscribe(Callback cb);\n    void publish(const ImuSample& sample);\n};\n```\n\nUse it for diagnostics, logging, and event fan-out. Be careful with callback lifetime and lock scope.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_4_plugin_and_factory_architecture",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.4 Plugin and Factory Architecture",
    "back": "A plugin interface defines behavior. A factory creates concrete implementations from configuration.\n\n```cpp\nclass Localizer {\npublic:\n    virtual ~Localizer() = default;\n    virtual void predict(double dt) = 0;\n    virtual void update(double z) = 0;\n};\n```\n\nFactories centralize construction and keep user code independent of concrete types.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_5_finite_state_machines",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.5 Finite State Machines",
    "back": "State machines are a good fit for drivers and lifecycle components: unconfigured, inactive, active, faulted. Represent states and events with `enum class`, and put transitions in one testable function.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_6_component_entity_systems",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.6 Component Entity Systems",
    "back": "Entity-component style separates identity from data:\n\n- Entity: ID\n- Component: data attached to an entity\n- System: logic operating on matching components\n\nThis can keep simulation or world-model code modular.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_7_mini_localization_architecture",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.7 Mini Localization Architecture",
    "back": "A small localization library usually needs:\n\n- State representation\n- Prediction model\n- Measurement update\n- History/logging\n- Reset and diagnostics\n- Clear invariants\n\nThe best architecture is boring: typed data, explicit ownership, testable transitions, and measured performance.\n\n---"
  },
  {
    "id": "08_robotics_patterns_concept_8_8_practical_rules",
    "module": "08_robotics_patterns",
    "moduleName": "Robotics Patterns",
    "type": "concept",
    "front": "Explain: 8.8 Practical Rules",
    "back": "- Use observer for event fan-out, not core ownership.\n- Use factories for runtime selection of known implementations.\n- Use FSMs for lifecycle and recovery.\n- Use component storage when many entities share optional data.\n- Keep math invariants close to the types that own the math."
  }
];
