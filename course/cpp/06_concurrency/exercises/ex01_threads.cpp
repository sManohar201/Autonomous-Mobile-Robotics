#include <cassert>
#include <iostream>
#include <thread>
#include <vector>

// TODO: launch N worker threads that each write their index into a vector.
// Join every thread before checking the vector.

int main() {
    std::cout << "ex01_threads passed\n";
}

