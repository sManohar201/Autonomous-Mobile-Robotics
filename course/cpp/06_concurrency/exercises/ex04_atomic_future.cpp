#include <atomic>
#include <future>
#include <iostream>

// TODO: use std::atomic<bool> as a stop flag.
// TODO: use std::async to compute a sum asynchronously.

int main() {
    std::cout << "ex04_atomic_future passed\n";
}

