#include <cassert>
#include <iostream>
#include <mutex>
#include <thread>
#include <vector>

// TODO: implement ThreadSafeCounter with increment() and value().
// Protect shared state with std::mutex and RAII locks.

int main() {
    std::cout << "ex02_mutex passed\n";
}

