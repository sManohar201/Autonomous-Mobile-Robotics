// Exercise 05 (Hard) - Mini Localization Library
//
// Combine all patterns from this module into a single Localizer1d:
//   - Observer: register callbacks notified after every state change
//   - Predict / update cycle with history tracking from the initial state
//   - Reset: override state to a given value, record, notify observers
//
// Design the public API yourself — there is no skeleton to follow.
// The combination of these patterns is common in real sensor fusion stacks.

#include <iostream>

int main() {
    std::cout << "ex05_hard passed\n";
}
